**Super1 — run-009 denetimi ve altıncı mühendis talimatı — 31 Ağustos 2026**

**Karar: NO_GO. `software_local_acceptance=INCOMPLETE`, `campaign_continuation=DESIGN_INCOMPLETE`.** Run-009'un `PASS/DESIGN_TESTED` etiketlerini sonraki koşuda devralma. Bu belge [talimat 05'in](C:/Users/ISAAC/Documents/otobacktestprojesi/docs/SUPER1_MUHENDIS_TALIMATI_05_20260831.md:1) yalnız açık kalan kabul maddelerini kesinleştirir; kapanan davranışları yeniden tasarlama.

Bağımsız doğrulanan kanıt: hedefli 163/163 ve tam 341/341 JUnit; failure/error/skip sıfır. “53/53”, 53 gereksinim-node eşleşmesidir; benzersiz zorunlu node sayısı 47'dir. [Run-009 manifesti](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-009/output-manifest.json:1), kendisi dışındaki 49/49 teslim dosyasını kapsıyor ve bütün SHA-256/byte değerleri mevcut dosyalarla eşleşiyor. Kayıtlı 27 girdi de eşleşiyor; manifest son yazılan dosyadır. Run-008 bağımsız hesapta 49 dosya ve `79e332068cfe6499f381d74bd5a4d2080de025e23e9c4a563c1aab9d6b55554c` ağaç hash'iyle korunmuştur. Bu bütünlük kazanımlarını geri alma. Mimar test, PowerShell, MT5, broker, SSH veya deployment çalıştırmadı; yalnız dosya/kanıt/kaynak denetimi yaptı.

Kapanan ve korunacak işler: R01 üretim gerçek saati + iki-cutoff guard + gerçek SPX ortak-vektör fixture'ı; R02'nin SDK öncesi atomik CANCEL_ARMED/outbox yazımı, restart'ta tek REMOVE ve global/son-candidate entry kapısı; C02 proximity+overnight-down ALLOW girdisinin 20004'e düzeltilmesi; gerçek Windows junction reddi; RSA/SHA256/PKCS1v15 ve ZIP doğrulamasının temeli; gerçek BarStore/emir tabloları + SQLite backup; production ve harness'in ortak failure orchestration fonksiyonunu çağırması; ayrı Python sürecinin gerçek fatal latch'i tüketmesi; son çıktı manifesti. Bunları bozma veya daha zayıf yardımcı testle değiştirme.

1. **Yetki sınırı değişmedi.**

   Yalnız Super1, aynı XM MT5 demo hesabı ve aynı mevcut kampanya; hedef Windows/SSH; diğer adayla bağımlılık yok. Hesap/server/magic, semboller, frozen engine/strateji/risk, 8 saniyelik closed-bar kuralı, iki bacak cutoff'u ve işlem pencereleri değişmez. `proven=false`, `parity=false` kalır; bunları teknik taşıma kabulünün hedefi yapma.

   Bu tur yalnız yerel kaynak/test/harness düzeltmesi, sentetik geçici state ve yeni kanıt koşusudur. Gerçek kaynak/hedef state veya snapshot, SSH, MT5 initialize/login, broker/emir, scheduled task, credential, kaynak durdurma, transfer/kurulum, paketleme ve gerçek imza yok. Mevcut lock/history bytes'ını değiştirme; init/reset/rollover ile yeni kampanya yaratma; normal daemon'un continuation protokolünü açma. Geçici TEST ONLY RSA/ZIP/broker fixture'ları dış sisteme çıkamaz.

   Run-009 ve önceki bütün koşular immutable kalır. Teslimi boş `run-010` altında üret; doluysa ilk sonraki boş run numarasını seç. Eski rapordaki yanlış PASS alanlarını geriye dönük değiştirme. Aşağıdaki yapılabilir yerel işlerden birini “gerçek ortam verisi yok” diyerek atlama; yalnız gerçekten kaynak snapshot gerektiren alanlar NOT_VERIFIED kalır.

2. **R02-P — Teyitli partial-remainder iptalini, açık pozisyonun ekonomik yaşamından ayır. Öncelik bir.**

   [Resolver](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:1556), herhangi bir entry deal bulunduğunda order history'deki teyitli remainder iptalini çözmeden ekonomik zincire giriyor. `.10` istekten `.04` korumalı pozisyon ve `.06` iptal edilmiş remainder kaldığında ekonomik zincir PARTIAL_FILL döndürüyor; control durumu CANCEL_UNKNOWN olarak kalıyor ve ancak `.04` pozisyon kapandıktan sonra çözülüyor. İptal sonucu ile pozisyon yaşamı iki bağımsız kanıttır.

   `_resolve_cancel_unknown` her readback'te iki sonucu ayrı hesaplasın:

   - **Cancel-control sonucu:** ticket/strategy order kimliği, original request, `volume_initial=.10`, entry deal toplamı `.04`, broker history order state CANCELED/EXPIRED/REJECTED ve aynı ticket'ın current orders'ta bulunmaması birlikte doğrulanırsa kalan `.06` için `CANCEL_RESOLVED` olayı yaz. History `volume_current` alanını veriyorsa değer iptal remainder'ı `.06` ile tutarlı olmalı; `0` şartı koyma. REMOVE ack veya pending'in yalnız kaybolması bu kanıt değildir. Kimlik/hacim çelişkisinde mevcut critical/unsafe engel korunur.
   - **Ekonomik sonuç:** aynı anda `.04` position görünür ve broker SL/TP doğruysa `broker_execution_states.state=PARTIAL_FILL`, `protection_state=PROTECTED` ve hacim `.04` olarak kalır. `OPEN_PROTECTED` tam dolum durumudur ve burada kabul edilmez. `order_intents.status=LINKED_EXISTING` olur; ekonomik lifecycle durumunu intent alanına yazma. `CANCEL_RESOLVED` olayı iptal edilen remainder, gerçekleşen hacim ve position_id bağlarını taşır. Bütün intent CANCELLED olamaz.
   - Cancel-control çözüldüğü için aynı ticket tekrar REMOVE edilmez. Açık/partial exposure mevcut olduğu için ekonomik unsafe/pair-risk kapıları yeni entry'yi yine engeller; iptal belirsizliğiyle ekonomik exposure blokajını birbirine karıştırma.
   - Pozisyon daha sonra gerçek `.04` SL veya TP çıkışıyla dengelendiğinde normal lifecycle CLOSED_SL/CLOSED_TP üretir. Terminal R, gerçekleşen pozisyonun frozen kuralıyla hesaplanır; istenen `.10` veya iptal edilen `.06` terminal hacim/R sayılmaz.

   Mevcut [R02-D testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_xm_mt5_forward.py:2435) cancellation history teyidi üretmiyor, private reconcile çağırıyor ve yalnız SL çalıştırıyor. Onu iki parametreli gerçek `reconcile_orders` kabulüne dönüştür:

   1. İlk döngüde `.04` entry deal + `.06` pending + korumalı `.04` position; kayıp REMOVE cevabı; toplam REMOVE=1, yeni-entry=0.
   2. Restart sonrası broker history aynı order ticket için terminal iptal state'ini doğrulasın; `volume_initial=.10`, entry-deal toplamı `.04`, iptal remainder `.06` olsun ve history `volume_current` sağlıyorsa `.06` ile tutarlı olsun. Current pending=0, position=.04. Cancel-control `CANCEL_RESOLVED`; `order_intents.status=LINKED_EXISTING`; `broker_execution_states.state=PARTIAL_FILL`, `protection_state=PROTECTED`; yeni-entry=0 ve REMOVE hâlâ 1 olsun. SLTP/position readback çağrıları gerçek trace'te görünsün.
   3. Ayrı `SL` ve `TP` parametrelerinde `.04` exit deal gelsin. Aynı gerçek reconcile yolunda CLOSED_SL/CLOSED_TP, dengeli hacim ve doğru terminal R oluşsun. Aynı order_id tekrar gönderilmesin; ilgisiz magic/position bağlanmasın.

   Eski kayıt kabulünü ayrıca tamamla. ARM olayı olmayan eski CANCEL_UNKNOWN ve CANCEL_REJECTED kayıtlarını, eski outbox'taki `ticket` alanıyla restart ederek test et. `request_json=NULL` iken request uydurulmayacak; yeni-entry=0 ve REMOVE=0 kalacak. Doğrulanabilir broker bağı gelince çözülür; çelişkili ticket critical/unsafe olur. Bu testler private helper değil gerçek döngü ve yeni client/store kullanır. R02 matrisine açık node/parametre olarak eklenir.

3. **T01, R03/T02 ve C02 davranış fixture'larını talimat 05'teki kesin sonuçlara getir.**

   **T01:** [Mevcut test](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:867) iki fetch'i aynı 10:28 sonuna götürüyor; karar listesinin boş olmadığını, stale→fresh 0→1 gönderimini veya SDK pending nesnesini kanıtlamıyor. Aşağıdaki tek zinciri kur:

   - NQ fetch gözlem zamanı 10:23:50, SPX fetch bitiş/gözlem zamanı 10:24:10 NY. Shared market-data-asof sabit kalsın; fiilî observation/first-known ve decision-produced zamanları geriye yazılmasın.
   - Gerçek iki fetch→BarStore→aggregation→prefix→frozen pair pipeline sonunda yalnız bir yürütülebilir NQ adayı olduğunu ve order_id/leg/pencere uygunluğunu reconcile'dan önce assert et. Boş karar listesi testi geçiremez.
   - Eski ortak prefix ile gerçek Super1 reconcile yeni-entry SDK=0; tamamlayıcı fetch ve iki cutoff'u güncel yeni prefix ile aynı aday toplam SDK=1. Stub gerçek pending broker nesnesi oluştursun ve ledger SUBMITTED readback'iyle eşleşsin. Tekrar ve yeni client'ta toplam 1.

   **R03/T02:** [Sınır testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_capital_forward.py:402) dört ayrı store kullanıyor ve sağlıklı 11:00:08'de yalnız `state != WAIT` diyor; DATA_INVALID da geçebilir. Aynı gün ve aynı gerçek store üzerinde sıralı çalıştır:

   - 10:58, 11:00:00, 11:00:07 shared-asof: kesin WAIT_FOR_FINAL_DATA; final attempt/session/marker yok.
   - Aynı store'a tamamlayıcı iki-bacak fetch'i ingest et; 11:00:08 shared-asof, gerçek finalization 11:05:10 NY ve sağlıklı tam veri: kesin VALID. `manual_required=false` açık test girdisi olabilir; DATA_INVALID/MANUAL_MISSING kabul edilmez.
   - Oluşan final/session/marker path ve hash'lerini kaydet. Aynı çağrı tekrarı ALREADY_FINALIZED dönsün ve bytes/hash'ler değişmesin.
   - Ayrı tam-kapsam fakat eksik veri ve çelişkili veri fixture'larında hedeflenen DATA_INVALID/critical nedenini assert et. Finalize/frame gate mock edilmez.

   **C02:** Proximity+overnight-down ALLOW verisi düzelmiştir; koru. [Mevcut assertion'lar](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:856) yalnız final state/send sayısına bakıyor. Dört senaryonun her birinde filter state, rule ve özellikleri ayrı assert et: Monday-short BLOCK; proximity+overnight-up BLOCK; `liquidity_type=vah_val_proximity + overnight_direction=down` ALLOW; önceki doğru RTH close eksik UNRESOLVED. Sayılar 0/0/1/0. ALLOW request'inin symbol/type/volume/price/SL/TP/magic/comment alanlarını, ledger ve broker pending readback'ini doğrula. BLOCK/UNRESOLVED kalıcı nedenini doğrula. Aynı prefix tekrarı ve yeni client/store toplamları değiştirmesin; başka prefix/permission hatasıyla alınan 0 geçmez.

   **Takvim negatifleri:** [Tam validator testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_calendar.py:89) ilerlemedir; ancak coverage boşluğu ve yanlış session saatini sınamıyor, yalnız herhangi bir check'in FAIL olmasını kabul ediyor. Duplicate key, tekrar tarih, coverage içi eksik tarih, coverage dışı tarih, yanlış timezone, normal/erken-kapanış için yanlış start/end ve provenance uyuşmazlığı ayrı parametre olsun. Her parametrede önceki kapılar geçsin ve tam `validate()` sonucunda beklenen belirli check adı/hata nedeni assert edilsin. Production takvim/hash/trust gevşetilemez.

4. **M01–M03 hâlâ DESIGN_INCOMPLETE; gerçek şema kabulünü tamamla.**

   **M01:** [Junction testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:310) kabul edildi. Aynı test grubuna şunları ekle: Windows'ta aynı source/output yolunun farklı harf büyüklüğü; source altına farklı-case yol; existing output. Her çağrıdan önce source envanterini `relative path + bytes + SHA-256`, existing output bytes/hash'ini al; ret sonrasında birebir aynı olduğunu doğrula. Case/junction kontrolü mkdir veya output open'dan önce çalışsın. Fixture kurulamıyorsa skip/PASS değil INCOMPLETE/fail.

   **M02:** [Fixture](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:71) gerçek lock'ta bulunmayan `campaign`/`account_server_binding` alanlarını ve bağımsız lock imzasını hâlâ kullanıyor. [Gerçek producer şeması](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_capital_forward.py:244) kullanılacak:

   - Geçici lock'u gerçek `campaign_lock` producer yoluyla oluştur; schema_version, created_at, parent/engine/config/runtime/harness/feed/epics/symbols/timeframes/execution ve gerçek XM account_login/server alanları dışında uydurma alan ekleme. Lock bağımsız imzalı olmak zorunda değildir. Validator lock bytes/hash ve semantiğini doğrular; onun değişmeyen SHA-256'sı imzalı transition payload içinde bağlanır. Production lock/pin/key değiştirilmez.
   - Bir genesis ve onu izleyen bir geçiş olmak üzere **iki tam kanonik transition** üret; ikisi de aynı dış TEST ONLY RSA köküyle imzalı olsun. Her halka tam `canonical_transition_payload` şemasına sahip olsun; `öncül.new == mevcut.old`, ortak lock/account/server/created_at, snapshot üyeleri, state şemaları ve değişmeyen engine/frozen/risk alanları zincir boyunca doğrulansın. Mevcut eksik [previous payload](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:141) kabul değildir. Genesis yalnız sentetik fixture içinde açıkça kanıtlanır; gerçek kaynak için genesis varsayılmaz.
   - Release fixture'ı gerçek builder manifest şemasını ve gerçek geçici ZIP üyelerini kullansın. `hashes.old/new.release`, ilgili `.manifest.json` dosyasının imzalanan **ham bytes SHA-256** değeridir; her farklı old/new release değeri için ayrı manifest+ZIP fixture'ı bulunur. Manifestin `archive_sha256` alanı ZIP ham bytes'ına, `files[path,sha256]` alanları ZIP üyelerine bağlanır. Aynı dış TEST ONLY RSA köküyle ham manifest bytes doğrulansın; profile/provenance ve artifact test envanteri kontrol edilsin. Sabit `"6"*64` gibi bağımsız değerler geçmez.
   - Semantik negatiflerde bütün alt bytes/hash/imzalar kendi içinde geçerli kalsın: başka account/server/lock; old-new kopması; release manifest/archive/üye uyuşmazlığı; snapshot üye kaybı; frozen/risk değişimi; eksik şema; yanlış trust root; tekrar halka ve yapısal cycle. Hedeflenen validator hata kodunu assert et. Dosyayı değiştirip eski imzada düşmek semantik zincir testi değildir.

   **M03:** [Snapshot testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:458) gerçek tablolar/backup içeriyor ancak WAL ve ekonomik negatifler eksik; validator hâlâ [SUPER1_TERMINAL_R](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:756) olayını ve `updated_at` tarifini kullanıyor.

   - Kaynak SQLite WAL modunda açıkken checkpoint edilmemiş son committed order/outbox/broker satırını oluştur ve WAL'da kaldığını ölç. SQLite backup snapshot bu satırı içersin. WAL'ı dışlayan ayrı bozuk kopya satırı kaybetsin ve validator belirli continuity hatasıyla reddetsin.
   - Sağlam snapshot'tan ayrı kopyalarda deal silme ve deal alanı değiştirme; outbox sequence, ack ve event içeriğini ayrı bozma; SUBMITTED/UNKNOWN/CANCEL_ARMED kaybı; zorunlu alan; terminal geçmişi; tutarlı farklı lock/account uygula. Her bozuk kopyanın kendi manifest hash'ini yeniden hesapla; kaynak-snapshot ekonomik/kimlik karşılaştırması yine reddetsin.
   - `SUPER1_TERMINAL_R` veya `updated_at` R kaynağı değildir. Manifest'e hash/byte ile bağlı açıkça sentetik `broker-history.json` ve `positions.json` sidecar'ları ekle; sabit observation time ve tam `terminal_history_days` kapsamını yaz. Sidecar deal alanları magic/entry/position_id/reason/symbol/time_msc/ticket, position alanları magic/ticket/identifier olsun.
   - Kaynak ve snapshot sidecar'larını bağımsız oku; aynı sabit saat altında gerçek [`_terminal_r_state`](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_xm_mt5_forward.py:486) metoduna yalnız `_mt5_collection` sınırından ver. İki tarafta terminal_count, `(time_msc,ticket)` sıralı son-10 R listesi, sum, scale ve deal/position kimlikleri hem birbirine hem önceden hesaplanan beklenen değere eşit olsun. Sidecar silme/değiştirme ve açık position değişimi negatif olsun. Gerçek snapshot/history/positions yokluğu NOT_VERIFIED kalır; boş geçmiş sayılmaz.

   Sentetik M01–M03 geçse bile gerçek source snapshot unresolved, `safe_to_apply/apply_allowed=false`; runtime continuation kabulü kapalıdır.

5. **O01 ve production rollover catch yolunu gerçek fail-closed sözleşmeye getir.**

   Ortak `Invoke-Super1FailureOrchestration` kullanımı ve gerçek Python latch kapısı kabul edildi. Şu somut hataları düzelt:

   - [Production catch](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:691) içindeki outer `if (-not $brokerSideEffectPossible)` altında tekrar `if (-not $persistentLatch)` var. Catch önce latch yazdığı için pre-broker güvenli rollback gövdesi ve [restart koşulu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:726) fiilen ulaşılamaz. İki politikayı açık ayır. Broker sınırından **önce** post-start latch orchestration'ı çalıştırma: runtime gerçekten durmuşsa staged app/state/candidate dosyalarını eski hash'lerine geri getir ve doğrula; görevler daha önce durdurulmuşsa değişmeyen eski task XML'iyle ana+watchdog görevlerini yeniden başlatıp eski health'i doğrula. Bu tam doğrulanmış güvenli rollback'te active fatal latch bırakma. Restore/stop/hash/health adımlarından biri kanıtlanamazsa persistent latch/recovery yaz, açık NO_GO üret ve restart=0. Broker sınırından **sonra** ise latch/recovery yaz, old/new ekonomik state'i koru, restore/restart=0. İç içe imkânsız koşul veya sessiz dead branch bırakma. Ortak catch-policy helper'ı iki dalı `brokerSideEffectPossible` girdisiyle belirlesin; harness hem bir pre-boundary güvenli rollback vakasını hem post-boundary vakaları aynı fonksiyonda çalıştırsın.
   - Post-start start-timeout/watchdog/stop/evidence-write senaryolarında ilk start/stop çağrıları ile **hata sonrası restart/broker çağrılarını** ayrı sayaçlarda tut. [Harness sonucu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_rollback_harness.ps1:126) eski sıfır değişkenleri döndürürken gerçek metrics/call-trace start=1 gösteriyor. Tek gerçek metrics kaynağını kullan; sonuç JSON, call-trace ve assertion aynı sayıları görsün. Post-start bütün vakalarda restart/broker-new-entry=0; ilk start girişimi dürüstçe 1.
   - Production archive failure writer'ını ortak helper sınırına çıkar. Harness gerçek aynı writer'ı kullansın; hedef dosya yerinde dizin/erişilemez hedef oluşturarak gerçek I/O hatası üret, yalnız scriptblock içinde `throw` yazma. Latch başarılı, archive kanıtı başarısız, health/recovery kalıcı ve restart=0 olmalı.
   - Ayrı `latch-write-failure` vakası ekle: `fatal_latch.json` hedefini gerçek dosya yazımını bozacak biçimde hazırla; orchestration Latch=null + açık NO_GO/errors üretir, stop/archive/health sonucu olduğundan iyi gösterilmez, restart/broker=0. Bu vaka “latch koruması kanıtlandı” PASS'i değil, doğru fail-closed NO_GO davranışının kabul testidir.
   - Boş başka state dizininde gate'in geçmesi bağlantı negatifi değildir. Catch politikasını brokerSideEffectPossible, stop/restore/archive/health/start scriptblock sınırlarıyla ortak helper'a taşı; production catch yalnız gerçek bağımlılıkları verip aynı fonksiyonu çağırsın, harness aynı fonksiyonu çalıştırsın. PowerShell AST kontratı production'ın bu çağrıyı doğru parametrelerle yaptığını doğrulasın. Ortak çağrıyı kaldırılmış izole production kopyasında kontrat testi FAIL olsun; asıl hata/karar algoritması production ve testte kopyalanmasın.
   - Production rollover ve ortak helper için ayrı PowerShell parser süreci ekle; parse error sıfır olmalı. Hiçbir gerçek betik ana akışı, scheduled task veya terminal çalıştırılmayacak.

   Post-start dört vaka + latch-write-failure için mevcut arşiv/ekonomik state dosyalarının önce/sonra envanter/hash'leri kaydedilsin. Yalnız önceden listelenmiş fatal/recovery/health/failure-evidence farklarına izin ver; diğer fark FAIL. Python `assert_no_fatal_latch` aynı geçici aktif state üzerinde yeni process'te latch nedeniyle fail etsin; başka config/import hatası kanıt değildir.

6. **V02/V03 — JUnit süresi değil, gerçek senaryo ölçümü kabulü belirlesin.**

   [Evidence producer](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_evidence_report.py:227) yalnız JUnit node varlığı/status/süresinden PASS üretiyor. [Run-009 observation](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-009/acceptance-matrix.json:67) sayısal entry/REMOVE, gerçek neden veya trace/ledger hash'i içermiyor; ayrı observation artefaktı yok. `expected.new_entry="SINGLE_REMOVE_ATTEMPT"` ölçülmüş `remove=1` değildir.

   Hafif bir ortak pytest observation yazıcısı ekle; yeni framework kurma. Koşu üreticisi targeted/full için ayrı create-new observation kökü ve `run_id/suite` ortamını versin. Her zorunlu node/parametre şu şemada tek JSON yazsın: schema, run_id, suite, exact nodeid, scenario_id, expected, actual, numeric `entry_sdk_calls/remove_sdk_calls/restart_calls`, kesin result/filter/guard/finalization reason, persistent intent/broker-state summary, ledger/trace/snapshot path+SHA-256 ve assertion status. Geçici ledger/trace kanıtını pytest tmp dizini silinmeden observation köküne create-new kopyala veya kanonik içerik+hash olarak bağla. Aynı node targeted/full koşusunda birbirini overwrite edemez.

   Salt calendar/path/crypto testinde emir alanları açık N/A olabilir. E/R/T/C emir senaryolarında sayılar, lifecycle state ve neden zorunludur; açıklama metni veya expected kopyası actual olamaz. Duplicate gereksinim eşleşmeleri aynı özgün observation'a hash ile bağlanabilir.

   Evidence producer şunların **hepsini** doğrulamadan ilgili madde PASS olamaz: iki suite JUnit PASS; tam doğru run/suite/node/scenario observation; observation hash/şema; numeric actual=expected; ledger/trace readback uyumu; ilgili state/reason. Ayrı üretici testlerinde (a) bir observation sil, (b) actual REMOVE/entry sayısını değiştir, (c) yabancı run/suite/node koy, (d) bağlı trace'i değiştir. Her biri report exit nonzero ve ilgili satır INCOMPLETE/FAIL üretmeli. Bu negatif testlerin kendisi de hedefli/tam suite envanterine girsin.

   Rapor `required_mapping_count=53` benzeri gerçek yeni sayıyı ve `unique_required_node_count` değerini ayrı hesaplasın; run-009 için bu değerler 53 ve 47'dir. Tüm full-suite node sayısını benzersiz zorunlu node diye yazma. Yeni node eklenirse her iki sayı gerçek envanterden yeniden hesaplanır; sabit 53/47 yeni kapı yapılmaz.

   Kapıları tek değişkende birbirine karıştırma. `software_ok`, E/R/T/C/O davranışları ile V02/V03 gözlemlerinden; `continuation_fixture_ok`, M01–M03 sentetik kabulünden; `evidence_integrity_ok`, zorunlu process exit'leri, iki JUnit, observation/trace doğrulaması, eksiksiz input envanteri, input before=after, predecessor before=after ve PowerShell parse/harness bütünlüğünden türesin. `local_acceptance_ok = software_ok AND continuation_fixture_ok AND evidence_integrity_ok`. Acceptance/readiness içindeki software ve campaign alanları kendi alt kapılarını dürüstçe göstersin; evidence alanını ve bölüm 7'de tanımlanan `preseal_integrity_ok` değerini ayrıca taşısın. Mühürlü dosyanın içine sonradan bilinebilen bir yayın sonucu yazma: authoritative `delivery_integrity_ok`, yalnız final run yolunun varlığı ve final bağımsız verifier sonucundan dışarıda türetilir; `run_evidence_ok = local_acceptance_ok AND delivery_integrity_ok` olur. Stdout'un nihai satırı ve process exit bu dış sonuçla aynı olsun. `overall=NO_GO`, gerçek ortam kapıları nedeniyle bundan bağımsız olarak değişmez. [Mevcut runner](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:390) readiness'i yalnız JUnit'ten, [çıkışı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:431) yalnız process+JUnit'ten çıkarıyor; bunu kaldır. Zorunlu dosyayı [sessizce atlayan](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:173) `if path.is_file()` davranışı fail olmalı.

   Girdi envanterine en azından zorunlu testlerin doğrudan kullandığı `live_forward/capital_demo_config.json`, `live_forward/xm_mt5_demo_config.json`, `forward_shadow/baseline_lock.json`, değişmiş `tests/test_deployment_security.py`, release builder/integrity helper'ları, yeni observation writer/testleri ve PowerShell parse kontratını ekle. Full suite'teki değişmemiş tracked bağımlılıklar git HEAD + temiz/explicit diff envanteriyle bağlanabilir; modified/untracked dosya yalnız isimle bağlanamaz. Önce/sonra eksik veya hash farkı acceptance=false yapar.

   **Önce-FAIL/sonra-PASS:** [Metadata alanı](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-009/process-metadata.json:205) aynı yeni bytes'ın önce/sonra eşit hash'idir; regresyon kanıtı değildir. Herhangi bir düzenlemeden önce run-009 manifestindeki ilgili mevcut bytes'ı salt okunur geçici baseline'a kopyala ve hash'i manifestle doğrula. Yeni test bytes'ını aynı tutarak, run-009 R02-partial/T01/R03/O01 bytes'ında hedeflenen assertion FAIL; düzeltilmiş bytes'ta PASS çalıştır. Çalışan ağacı geri alma. Baseline/current production hash, tek test hash, komut, exit, ham stdout/stderr ve hedeflenen failure ayrı bağlansın. Tam bağımlılık seti yeniden kurulamazsa o karşı kanıt INCOMPLETE; aynı hash'i before/after diye etiketleme.

7. **Run-010 teslimi ve doğru karar alanları.**

   Output manifest kazanımını koru ve teslimi iki aşamada mühürle. Final run yolunu baştan oluşturma; aynı volume'da create-new geçici sibling dizinde bütün nihai rapor/observation/trace/snapshot/stdout/stderr/JUnit dosyalarını yaz. Final non-manifest bytes envanterini 1:1 doğrulayıp raporlara `preseal_integrity_ok` olarak yaz; geçici veya `.failed` dizindeki bütün raporlar, alanları PASS olsa bile açıkça `PROVISIONAL_NOT_PUBLISHED` ve geçersizdir. Ardından manifesti geçici dizinin tek son dosyası olarak oluştur; kendisi dışındaki teslimin tamamını relative path+bytes+SHA-256 ile, ayrıca input hash'leri ve immutable run-009 predecessor ağaç hash'iyle kapsasın. Manifestten sonra bağlı veya mühürlü hiçbir byte'ı yeniden yazma. Ayrı verifier manifesti ve tüm üyeleri yeniden okuyup 1:1 doğrulamazsa geçici dizini `.failed` olarak bırak/yeniden adlandır, exit nonzero ver ve `run-010` yayımlama. Başarılıysa tüm dizini boş final run adına atomik rename et, final yolda bir kez daha bağımsız doğrula; bu son doğrulama başarısızsa final dizini `.failed` adına geri çek, process içindeki `delivery_integrity_ok/run_evidence_ok=false` olur, exit nonzero verilir ve geçerli run yolu kalmaz; sealed raporu false'a çevirmeye çalışma. Yalnız final yol hâlâ mevcut ve son verifier PASS ise tüketici `delivery_integrity_ok=true` ve `run_evidence_ok=local_acceptance_ok` türetir; final run dizininin varlığı commit işaretidir. Böylece mühür öncesi sonuç geçerli teslim PASS'i sayılmaz ve manifest yine teslimde son dosyadır.

   Mevcut [continuation proposal](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-009/continuation-contract-proposal.json:1) yalnız üç genel satırdır. Paket üretmeden somut successor release önerisi yaz: kesin payload source/helper/calendar/provenance dosyaları; kesin artifact test dosya/node envanteri; son gerçek targeted/full toplamları; manifest/archive/üye/RSA doğrulama sözleşmesi; eski v16 268/139 kapısının ve paketlerinin değişmediği. Status REVIEW_REQUIRED/NOT_BUILT/NOT_SIGNED olur; `>=` kapısı, paket veya imza yok.

   Readiness ve acceptance aynı teknik alanları açıkça taşısın:

   | Alan | Run-010 için izin verilen sonuç |
   | --- | --- |
   | software_local_acceptance | Bu belgedeki E/R/T/C/O ve V02/V03 davranışları eksiksizse PASS; aksi INCOMPLETE/FAIL |
   | campaign_continuation | M01–M03 tam olumlu/olumsuz fixture kabulü varsa DESIGN_TESTED; aksi DESIGN_INCOMPLETE |
   | evidence_integrity / preseal_integrity | Kendi kapılarından PASS/FAIL; geçici dizinde her zaman PROVISIONAL_NOT_PUBLISHED |
   | delivery_integrity / run_evidence | Sealed rapora sonradan yazılmaz; yalnız yayımlanmış final yol + final verifier sonucundan authoritative olarak türetilir |
   | campaign_mode / runtime_state | CURRENT_SUPER1_CAMPAIGN_CONTINUATION / NOT_DEPLOYED; “fresh/new campaign” etiketi yok |
   | source_fenced / source_snapshot / target_environment | NOT_VERIFIED |
   | broker_preflight / pending_acceptance / fill_exit / task_reboot / deployment | NOT_RUN |
   | release_contract | REVIEW_REQUIRED; NOT_BUILT; NOT_SIGNED; somut envanterli öneri |
   | safe_to_apply / apply_allowed / proven / parity | false |
   | overall | NO_GO |

   NO_GO reason yalnız “profitability/parity kanıtlanmadı” olamaz. Kalan yerel teknik açıkları ve source/target/broker/task/deployment NOT_RUN kapılarını ayrı listele. `proven/parity=false` korunur fakat teknik taşıma için kârlılık kanıtı şartı icat edilmez. Pending kabulü dolum/çıkış garantisi değildir; hiçbir yerel fixture kesintisiz çalışma veya broker dolumu garantisi vermez.

   Bu teslimden sonra kendiliğinden SSH, snapshot, kurulum veya broker aşamasına geçme. Yerel kabul gerçekten kapanırsa mimar sonraki raporda yalnız gerçek ortam preflight planına geçilip geçilemeyeceğine karar verecek. Bilinmeyen gerçek değerleri uydurma ve başka bir adayla ortak dependency ekleme.
