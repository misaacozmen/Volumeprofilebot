**Super1 — run-008 denetimi ve beşinci mühendis talimatı — 31 Ağustos 2026**

**Karar: NO_GO. Yerel yazılım kabulü tamamlanmadı; continuation tasarım kabulü de eksik.** Öncelikli hata R02: kalıcı iptal belirsizliği sonraki döngüde yeni emri engellemiyor, aynı REMOVE tekrarlanabiliyor, partial pozisyon normal uzlaştırmadan çıkarılıyor. Aşağıdaki işleri sırayla tamamla; rapordaki PASS etiketini korumak için kabul koşullarını değiştirme.

Bu belge [talimat 04'ün](C:/Users/ISAAC/Documents/otobacktestprojesi/docs/SUPER1_MUHENDIS_TALIMATI_04_20260831.md:1) **kalan maddelerini** kesinleştirir; kapanan işleri yeniden tasarlama. Bağımsız incelemede 149 hedefli/327 tam JUnit testi, hata/skip olmaması, 26 girdi hash'i ve run-007'nin 90 dosyalık ağaç hash'i doğrulandı. “44/44” gereksinim–node eşleşmesidir; 38 benzersiz zorunlu node vardır. Bu sayımlar doğru olmakla birlikte testlerin eksik davranış kapsamını tamamlamaz. Mimar test/uygulama/sunucu işlemi çalıştırmadı.

Korunacak ilerleme: R01'in gerçek son gönderim saati ve iki-cutoff üretim kontrolü; R03'ün kullanılabilir closed-cutoff kontrolü; E01/E02, normal E03 ve ekonomik broker nesnesi oluşturan E04; bağımsız takvim karşılaştırması; Super1 harness hash sırası; dış test güven kökünden RSA doğrulaması; gerçek SQLite şeması/backup/readback; ortak fatal-latch yazıcısı ve ayrı Python sürecindeki gerçek latch engeli. Aşağıda sadece bunların kalan hata veya kabul açıkları vardır.

1. **Yetki ve değişmezler.**

   Yalnız Super1; aynı XM MT5 demo hesabı ve aynı mevcut kampanya; hedef Windows/SSH; diğer adaydan tamamen bağımsız. Hesap/server/magic, semboller, frozen strateji/engine/risk, 8 saniye kuralı ve iki bacağın pencere/cutoff sözleşmesi değişmez. Kullanıcıdan bu kararları yeniden isteme.

   Yalnız yerel hedefli kod/test düzeltmesi ve yalıtılmış sentetik kanıt üret. SSH, gerçek kaynak/hedef okuma veya snapshot, MT5 initialize/login, broker/preflight/emir, scheduled task, credential, kaynak durdurma, transfer/kurulum ve gerçek paket/imza işlemi yapma. Normal daemon'un continuation kabulünü açma. Lock/history bytes'ını değiştirme; init/reset/rollover ile yeni kampanya yaratma. PowerShell yalnız sentetik harness içinde, gerçek dış yan etki sınırları kapalı çalışabilir. Geçici TEST ONLY RSA/ZIP fixture'ı gerçek release üretimi değildir.

   Önceki talimatlar ve run-008 dahil eski çıktılar değişmez. Yeni çıktıyı boş `run-009` altında üret; doluysa ilk sonraki boş numarayı kullan, eski dosyaya yazma. Rutin yardımcı fonksiyon düzenlemeleri bu kapsam içinde olabilir; kabul davranışı, izin sınırı veya strateji hakkında alternatif karar alamazsın.

2. **R02 — İptal kontrolünü ekonomik durumdan ayır; önce bunu düzelt.**

   Kaynak: [kalıcı intent dalı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:1230), [current/deal görünce sürekli UNKNOWN dönüşü](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:1312), [yeniden REMOVE çağrısı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:2420), [global giriş kapısı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:2627). Son kapı yalnız yeni cleanup sonuçlarına bakıyor; daha önce kaydedilmiş CANCEL_UNKNOWN/CANCEL_REJECTED kapsam dışı kalıyor.

   Mevcut [üç tabloyu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_xm_mt5_forward.py:399) kullan; yeni DB şeması veya gerçek state dönüşümü açma. `order_intents.status` iptal kontrolünü, `broker_execution_states` gerçek order/deal/position/protection durumunu taşısın. Kalıcı iptal girişimi/sonucu `order_event_outbox.event_json` içinde aynı `order_id + broker_order_ticket` kimliğine bağlansın. `request_json`, orijinal ticket, hacim ve pozisyon bağlarını koru; ekonomik kaydı yalnız CANCEL_UNKNOWN yazıp ezme. Ack edilmiş outbox kaydı da önceki girişimin kanıtıdır.

   Uygulama sırası ve durumları:

   - `_remove_order`: SDK'dan önce `BEGIN IMMEDIATE` içinde aynı ticket için iptal girişimi bulunmadığını denetle; `CANCEL_ARMED` ve girişim olayını aynı transaction'da commit et. Sonra bir kez REMOVE gönder. Commit sonrasında süreç ölürse gönderim yapılmadığı varsayılmaz. Önceki girişim veya CANCEL_ARMED varsa tekrar SDK çağırma; readback ile devam et.
   - DONE cevabı → `CANCEL_ACKNOWLEDGED`; None/exception/timeout/connection veya okunamayan sonuç → `CANCEL_UNKNOWN`; açık ret → `CANCEL_REJECTED`. Kabul cevabı tek başına flat/CANCELLED kanıtı değildir. Belirsizlik/ret nedeniyle fatal izleme kesintisi veya otomatik ikinci REMOVE üretme. Mevcut kimlik/bütünlük ihlallerinin kritik engeli korunur.
   - `_reconcile_persistent_intents`: iptal durumundaki intent'i `continue` ile ekonomik denetimden çıkarma. Her döngüde aynı intent için gerçek order→deal→position/SLTP→exit zinciri ve iptal readback'i çalışsın. Cancel kontrolü ancak kimliği/hacmi tutarlı broker kanıtıyla `CANCEL_RESOLVED` olur; doğrulanan iptal/dolum/sona erme sonucunu ayrıca kaydet. Pending listesinde görünmemek tek başına çözüm değildir. Yeni kayıtlara geçişte mevcut CANCEL_UNKNOWN/CANCEL_REJECTED kayıtları da güvenli biçimde uzlaştırılsın; silinmesin. Eski CANCEL_UNKNOWN/CANCEL_REJECTED, CANCEL_ARMED olayı bulunmasa da önceki iptal girişiminin kanıtıdır; tekrar REMOVE yapılmaz. Eski outbox `ticket` alanını mevcut broker ticket kimliğiyle ilişkilendir; çelişkili kimlik geçemez. `request_json` eksikse request uydurma; doğrulanabilir ekonomik bağ kurulana kadar yeni-entry engelini koru, terminal durum üretme.
   - Global yeni-entry kapısı kalıcı `CANCEL_ARMED / CANCEL_ACKNOWLEDGED / CANCEL_UNKNOWN / CANCEL_REJECTED` kayıtlarını kapsasın. Kontrol hem döngü başında hem her adayın son gerçek saat guard'ından önce yapılsın. Sadece o döngünün cleanup listesine veya aynı adayın order_id'sine bakmak yetmez. Güvenlik/korunma izlemesi açık kalır; aynı ticket'ın cleanup'ta yeniden görünmesi ikinci REMOVE üretmez. Aynı strateji order_id'si iptal çözülünce de yeniden gönderilmez. Eski unsafe/unknown/ownership kapıları gevşetilmez.
   - `.10` talebin `.04` dolumu + `.06` teyitli iptali, `.04` pozisyonun kapanışı değildir. Koruma ve sonraki `.04` çıkış normal zincirde izlenir. Kalan hacmin iptali doğrulanmış ve giriş/çıkış dengelenmişse terminal SL/TP değerlendirmesi gerçek dolan hacme göre tamamlanabilsin; sırf `.04 < .10` diye sonsuza kadar PARTIAL_FILL kalmasın. Açık/korumasız/uyuşmazlık taşıyan pozisyonda yeni-entry engelleri korunur. Ekonomik CANCELLED yalnız hiç giriş gerçekleşmediği doğrulanınca kullanılabilir; R hesabının frozen yöntemi değişmez.

   **Zorunlu kabul:** mevcut [R02 testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_xm_mt5_forward.py:2029) görünürlüğü açtıktan sonra private resolver çağırıyor; gerçek belirsiz restart döngüsünü sınamıyor. Aşağıdaki senaryolar gerçek `reconcile_orders`, ledger, yeni istemci/bağlantı ve broker sınırı üzerinden çalışacak; resolver/reconcile/protection mantığı mock edilmeyecek.

   | Senaryo kimliği | Fixture ve ölçülecek sonuç |
   | --- | --- |
   | R02-A | Sahipliği kesin pending için REMOVE gerçekten uygulansın; cevap None, exception, timeout-retcode, connection-retcode olarak ayrı parametrelerde kaybolsun. History henüz görünmezken aynı store ile yeni istemci ve VALID başka aday çalışsın: yeni-entry=0, toplam REMOVE=1, fatal yok. Sonra doğrulanan iptal readback'iyle çözülsün. |
   | R02-B | Cevap kaybından sonra pending hâlâ görünür olsun. İkinci DATA_INVALID cleanup ve restart döngüsü çalışsın: toplam REMOVE yine 1; yeni-entry=0. |
   | R02-C | CANCEL_ARMED commit sonrasında, SDK çağrısından önce süreç kesintisini enjekte et. Yeni istemci aynı girişimi tekrar göndermez; çağrı toplamı 0, belirsizlik kalıcı, yeni-entry=0. |
   | R02-D | `.04` dolum/korumalı pozisyon + `.06` belirsiz iptal; belirsizlik sürerken SLTP/pozisyon readback çağrılarını göster. İptal teyidinden sonra pozisyon izlenmeye devam etsin; ayrı SL ve TP fixture'larında dengeli çıkıştan sonra doğru terminal ekonomik durum oluşsun. İlgisiz magic/position korunur. |
   | R02-E | Kesin ret ayrı durum olarak kalıcıdır; yeni-entry=0 ve kör retry yoktur. Broker teyidiyle çözüm senaryosu çalışır. Hiç dolmamış teyitli iptalden sonra farklı, tüm kapıları uygun yeni adayın gönderimi=1 olsun; eski order_id yeniden gönderilmesin. |

   Sayaçlarda başlangıç emrinin kurulması ile test edilen iptal/sonraki-entry evrelerini açıkça ayır; ham çağrı izini sakla. A/B'nin en az bir regresyonu run-008 production bytes'ında beklenen davranış assertion'ından FAIL, düzeltmede PASS göstermeli. Import/config hatası bu karşı kanıt değildir.

3. **R01/T01/R03/C02 — Düzelmiş üretim kurallarını koru; kabul fixture'larını tamamla.**

   | Madde ve mevcut eksik | Yapılacak tek kabul yolu |
   | --- | --- |
   | R01: [vektör testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_xm_mt5_forward.py:1932) NQ adayı kullanıyor; kendi bacağı eski olduğu için eski tek-bacak guard'ı da geçebilirdi. | SPX adayı kullan. Saat 10:24:10 NY; prefix NQ=10:21/SPX=10:20 → send=0. NQ=10:24/SPX=10:20 ile yeni ortak prefix → send=1; toplam 1. Talimat 04'teki order_check/izin/outbox gecikmesiyle NQ 10:30 aşımı ve 10:35 uygun SPX kabulünü koru. Guard veya üretim çağrısına sabit `send_now` ekleme. |
   | T01: [mevcut birleşik test](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:797) tek sabit 10:28 döngüsüdür; candidate_count=boş karar sayısı ile geçebilir. | NQ fetch 10:23:50, SPX fetch sonu 10:24:10 NY. Gerçek iki fetch→BarStore→aggregation→prefix→frozen karar üretimi→Super1 reconcile çalışsın; gerçekten yürütülebilir yalnız NQ adayı olduğunu önce assert et. Korunan shared-asof ile ilk tur stale-prefix send=0; sonraki tamamlayıcı fetch/güncel ortak prefix ile send=1. Gerçek observation/first-known ve decision_produced_at ilerlesin, shared-asof geriye dönük değiştirilmesin. SDK stub'ı ekonomik pending nesnesi oluştursun. Tekrar/yeni istemci duplicate üretmesin. |
   | R03/T02: [sağlıklı sınır assertion'ı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_capital_forward.py:473) yalnız `state != WAIT` diyor; DATA_INVALID da geçiyor. | Aynı gerçek store/gün üzerinde shared-asof 10:58, 11:00:00, 11:00:07 → WAIT, final attempt/marker yok. Sonraki gerçek tamamlayıcı fetch ile 11:00:08, iki bacak tam/sağlıklı veri ve diğer gerekli fixture girdileri → kesin VALID; final session/marker bir kez, tekrar ALREADY_FINALIZED ve bytes değişmez. Gerçek finalization saati 11:05:10 NY. Ayrı tam-kapsam/eksik-veri ve çelişkili-veri fixture'larında mevcut DATA_INVALID/critical nedeni doğrulansın. Finalize/frame gate mock edilmez, sağlıklı fixture DATA_INVALID/MANUAL_MISSING ile geçemez. |
   | C02: [proven-overnight-allow](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:568) hâlâ 20030 kullanıyor; `other_liquidity` olduğu için overnight dalı atlanıyor. | Aynı VAH=20100/VAL=20000 fixture'ında proximity fiyatı 20004 kullan; gerçek önceki RTH kapanış/açılış barları overnight=down üretsin. Gerçek filter sonucunda liquidity_type=vah_val_proximity ve overnight_direction=down, ardından ALLOW ve 1 send doğrulansın. Mevcut Monday-short BLOCK, proximity+up BLOCK ve eksik doğru kapanış UNRESOLVED ile toplam dört senaryo 0/0/1/0 kalsın. |

   C02'nin her senaryosunda kesin filter/NO_SEND nedeni, gerçek kalıcı karar/olay kaydı ve uygun ALLOW request'inin symbol/type/volume/price/SL/TP/magic/comment içeriğini doğrula. Aynı prefix tekrarı ve yeni istemcide toplam sayılar değişmesin. Filtre/risk/request/guard mock edilmez; başka bir izin/eski-prefix hatasından alınan 0 geçmez. Takvim gerekmeyen `other_liquidity` dalı ayrıca korunur; ALLOW kabulünün yerine sayılmaz.

   **C01/C02 takvim negatifleri:** mevcut hash ve karşılaştırma helper testlerini koru; [test_super1_calendar.py](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_calendar.py:50) içindeki eksik parser/semantik kabulü tamamla. Duplicate JSON key, tekrar tarih, coverage boşluğu/dışı, yanlış timezone/saat ve provenance uyuşmazlığı ayrı fixture'lar olsun. Her birinde önceki hash/JSON kapılarını gerektiği gibi geçerek tam `validate()` girişinden hedeflenen somut nedenin FAIL sonucunu al. Kaynak semantik mutasyonu yalnız compare helper'ında değil tam validator'da reddedilsin. Yalnız geçici fixture'ın beklenen hash'i yeniden hesaplanabilir; production config/hash/trust gevşetilemez. Talimat 04'teki tatil, erken kapanış ve DST örnekleri korunur.

4. **M01–M03 — Gerçek şemaya bağlı continuation fixture'ını tamamla.**

   **M01:** Super1 harness kapsamı ve bilinmeyen kaynak alanlarının null kalması kabul edildi. [Mevcut “reparse” testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:245) yalnız existing-output/containment gösteriyor. Geçici kaynakta Windows case eşdeğerliği, junction/reparse üzerinden kaynak içine çıktı yönlendirme ve existing-output için gerçek ret testleri ekle. Kaynak dosya envanteri/hash'leri ve mevcut çıktı bytes'ı önce/sonra aynı kalsın. Reparse fixture kurulamadıysa PASS değil INCOMPLETE yaz; gerçek kaynak kökünü kullanma.

   **M02:** RSA bytes doğrulaması ve signed-payload/record eşitliği kabul edildi. Ancak [validator](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:362) root/release'in iç anlamını bağlamıyor; [öncül](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:395) imzasız ve yalnız yüzeysel hash kontrolünde. [Olumlu fixture](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:81) tam transition yerine `predecessor_hash=null`, release yerine TEST_ONLY etiketi kullanıyor.

   Burada önceki talimattaki “campaign/root” ifadesini **gerçek şemayla** uygula: [campaign_lock üreticisi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_capital_forward.py:244) bağımsız imza veya `campaign` alanı üretmiyor. Bunları var sayma, gerçek lock'a ekleme ve zorunlu `campaign_lock.sig` şartı koyma. Kampanya kökü, değişmeyen ham lock SHA256 + gerçek created_at/account_login/server + mevcut mühür alanlarıyla bağlanır; ham lock hash'i imzalı transition içinde yer alır. Test fixture'ı da üreticinin gerçek şemasını kullansın.

   - Release fixture'ı [manifest üreticisinin](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/build_signed_windows_release.ps1:333) ve [Assert-SignedReleaseArchive sözleşmesinin](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/release_integrity.ps1:35) şemasına uysun: geçici ZIP, archive_file/archive_sha256, files[path,sha256], profile/provenance ve ham manifest bytes üzerinde RSA/SHA256/PKCS1v15. Manifest/ZIP/üyeler transition'ın ilgili eski/yeni release bağlarıyla karşılaştırılsın. Test güven kökü dışarıdan verilir; production pin/özel anahtar değişmez. Sentetik provenance gerçek release doğrulaması sayılmaz.
   - Kökten başlayan en az iki tam şemalı, imzalı sentetik transition kullan. Her halkada imza, aynı orijinal lock/hesap/tarih, öncül.new == mevcut.old hash bağları, snapshot manifest/üyeleri ve değişmeyen engine/frozen/risk doğrulansın. Önceki geçişin yokluğu yalnız fixture'da açıkça kanıtlı genesis olabilir; gerçek kaynak bilinmiyorken genesis uydurma.
   - Ayrı negatifler: record/payload uyuşmazlığı; içeriği başka hesap/kampanyaya ait ama kendi hash/imzası doğru root/release; old/new bağlantı kopması; eksik şema/hash; snapshot üye kaybı; yanlış güven kökü/imza; frozen/risk değişimi. Semantik negatiflerin önceki byte/imza kapıları geçerli olsun ve hedeflenen ilişki hatası açıkça assert edilsin. Önceki ham-byte bozulması testleri ayrıca korunur.
   - Öncül eksikliği/tekrarı/çevrimi zincir gezintisinde denetlensin. Kriptografik hash çevrimi üretmeye çalışma: yol/kayıt tekrarını hedefleyen yapısal fixture'da ilgili cycle/duplicate nedeni doğrulansın. Sadece dosyayı değiştirip eski hash/imzada düşen test “çevrim doğrulandı” sayılmaz.

   **M03:** Gerçek şema, backup/readback ve beş veri mutasyonu kabul edildi. [Fixture](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:293) ve [karşılaştırıcı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:564) şu açıkları kapatacak:

   - Kaynakta hem UNKNOWN hem gerçekten gönderilmiş/SUBMITTED intent, ack'li/ack'siz sıralı outbox, broker execution satırları, bar/conflict ve deal kayıtları olsun. Yeni R02 iptal girişim/durum olayları da snapshot'ta kayıpsız kalsın. SQLite WAL açıkken son committed satırın ana DB'ye checkpoint edilmediğini ölç; geçerli backup o satırı içersin. WAL'ı dışlayan ayrı bozuk kopya bu son satırı gerçekten kaybetsin ve validator reddetsin.
   - Sağlam snapshot'tan ayrı kopyalarda deal silme/değiştirme, outbox sequence/ack/içerik bozulması, terminal geçmişi kesme, zorunlu alan kaybı ve kendi içinde tutarlı farklı kampanya uygula. Snapshot'ın kendi hash manifesti yeniden hesaplandığında da kaynakla ekonomik/kimlik karşılaştırması kaybı bulsun. Önceden verilmiş blocked/PASS bayrağı veya boş veri listesinin all([]) sonucu kullanılmaz.
   - Risk hesabını [gerçek `_terminal_r_state`](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_xm_mt5_forward.py:486) belirler: kendi magic'i, DEAL_ENTRY_OUT, artık açık olmayan position, SL=-1/TP=frozen reward_r, `(time_msc,ticket)` sırası ve son 10. `updated_at`, P&L veya üreticinin yazmadığı SUPER1_TERMINAL_R olayından yeni yöntem türetme; [envanterdeki yanlış tarifi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:456) düzelt.
   - 10'dan fazla terminal sonucu olan fixture'da manifest'e hash ile bağlı, açıkça sentetik broker-history ve positions kanıt dosyaları oluştur; sabit gözlem zamanı ve frozen `terminal_history_days` kapsamı kaydedilsin. Kaynak ve snapshot'ın bağımsız readback'inden ham deal/position girdilerini aynı saat altında gerçek `_terminal_r_state` metoduna ayrı ayrı ver; yalnız `_mt5_collection` sınırı sentetik olabilir. Terminal count, son-10 R listesi, toplam/ölçek ve deal/position kimlikleri hem birbirine hem önceden belirlenmiş beklenen değerlere eşit olsun. Mevcut DEAL günlüğü açık positions snapshot'ı içermez; 35 günlük senkronizasyon 365 günlük R geçmişinin tamlığını kanıtlamaz. Gerçek snapshot/history/positions eksikliği NOT_VERIFIED kalır; boş geçmiş/pozisyon veya yeni kampanya sayılmaz. 10'dan fazla sonuç yalnız test kapsamıdır, gerçek kampanyaya yeni önkoşul değildir.

   Bu fixture'lar başarılı olsa bile gerçek continuation `safe_to_apply=false`, `apply_allowed=false`, `SOURCE_SNAPSHOT_NOT_VERIFIED` kalır. Normal runtime'ın yeni protokolü kabul etmesi bu işin kapsamı değildir.

5. **O01 — Ortak latch yazımı yeterli değil; production hata yönetiminin tamamını sınat.**

   [Harness](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_rollback_harness.ps1:38) hâlâ kendi switch/catch yolunu ve elle artırılan start/stop sayaçlarını kullanıyor; evidence-write-failure gerçek dosya yazımında oluşmuyor. Production yalnız [latch yazıcısını](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:626) paylaşıyor. Ayrı Python sürecinde gerçek CriticalLiveError görülmesi geçerlidir; harness stderr'ündeki beklenen bu hata test arızası sayılmaz.

   Talimat 04'te istendiği gibi production post-start/catch/recovery **orchestration** sınırını dar ortak fonksiyona çıkar; gerçek betik ve harness aynı yolu kullansın. Sadece latch yazıcısı veya production dosyasında dot-source metni bulunması bu kabul değildir. Scheduler/process/broker/I/O bağımlılıklarını sınırda değiştir; catch/karar/durum üretimini mock etme. Gerçek betiğin ortak yolu çağırdığı davranış testi de olsun; girişte gerçek görev/terminal çalıştırılmasın.

   Start timeout, start sonrası watchdog hatası, stop hatası ve gerçek archive evidence-write hatası ayrı sınır enjeksiyonları olsun. Gerçek çağrı izinden ilk start/stop ve hata sonrasındaki yeniden start sayıları hesaplanır. Post-start belirsizlikte restore/restart=0; STOPPED yalnız gerçek sınır kanıtıyla. Arşivdeki mevcut ekonomik/kimlik kanıtları ve old/new ekonomik state bytes'ı korunur; yalnız adı/rolü önceden listelenmiş fatal/recovery/health/hata kanıt dosyalarının eklenmesi veya hata durumuyla güncellenmesi beklenir. Diğer dosya farkları reddedilir. Archive yazımı başarısızken latch yine kalır; latch de yazılamazsa güvenli yeniden başlama kanıtı yoktur, NO_GO raporlanır.

   Yeni yalıtılmış Python process **aynı geçici state** üzerinden gerçek `assert_no_fatal_latch` kapısında, beklenen latch nedeniyle engellensin; bu yeniden başlama evresinde broker/start=0 olsun. Negatif kontrol, izole kopyada production→ortak hata yolu veya startup→latch kapısı bağlantısını kaldırınca kabul testinin FAIL olduğunu göstermeli. Boş başka state dizininde kapının geçmesi, bağlantı kopması testi değildir. Production bypass, otomatik recovery veya gerçek rollover çalıştırma yok.

6. **V02/V03 — PASS, ölçülen davranış ve bütünlükten türetilecek.**

   [Node ayrıştırıcısının](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_evidence_report.py:214) missing/non-PASS kontrolü kabul edildi. Fakat [new_entry/remove/persistent_state](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_evidence_report.py:235) hâlâ tanımdan kopyalanan sabit metin; örneğin SINGLE_REMOVE_ATTEMPT, ölçülmüş 1 değildir. Gereksinim kabulü yalnız node adından çıkarılamaz.

   Koşudan önce bu belgedeki her alt senaryoyu gerçek pytest node/parametre ve beklenen assertion'larla eşleştir. Mevcut test kapsamını silme/gevşetme; ALLOW fixture parametresi gibi haklı node değişikliklerini eski→yeni eşlemesiyle kaydet. Her yürütme için ayrı gözlem kaydı üret: run/suite/node/scenario, gözlenen saat/cutoff, sayısal SDK entry/REMOVE çağrıları, çağrı izinin hash/path'i, kesin filter/guard/finalization nedeni, bağımsız ledger/snapshot okuması ve hash/path. Expected ve actual ayrı kaynaklardan gelir; actual beklenen değerin kopyası olamaz. Emirle ilgisiz imza/yol testleri için dürüst N/A kullanılabilir; emir senaryolarındaki eksik sayıyı N/A veya açıklama metniyle örtme.

   Aynı node hedefli/tam suite'te çalışınca gözlem dosyası overwrite edilmez. Eksik/skipped/xfail node, boş senaryo, eksik/bozuk/yabancı koşuya ait observation, yanlış ölçüm veya hash uyuşmazlığı ilgili maddeyi FAIL/INCOMPLETE yapmalı. Bir gözlemi silen ve bir actual sayıyı bozan ayrı yerel negatiflerde rapor üreticisinin reddettiğini göster. JUnit PASS tek başına bu negatifleri geçiremez. Benzersiz node sayısını gereksinim eşleşmesi sayısından ayrı raporla.

   [Run üreticisinde](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:303) hash eşitlikleri yalnız metadata'ya yazılıyor; [readiness PASS](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:343) yalnız JUnit sonucuna, [çıkış kodu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:369) süreçler+JUnit'e bağlı. Yeni koşuda tek ortak kabul hesabı kullan: tüm zorunlu süreçler başarılı, tüm alt senaryoların ölçümleri uygun, zorunlu girdiler eksiksiz, önce/sonra girdi envanteri ve hash'leri aynı, eski çıktı bütünlüğü aynı. Hiçbiri yalnız bilgi alanı olmasın. Bu kapılardan biri başarısızken readiness/matris PASS veya başarılı yerel kabul çıkış kodu üretilemez. Çalıştırılmamış işlem için sahte başarı yazma.

   İki farklı kanıtı ayır: koşu sırasında dosyanın değişmemesi ile eski hatanın yeni regresyonda görünmesi. [before_failure_after_success alanı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:306) şu anda aynı bytes'ın koşu öncesi/sonrası hash'ini gösteriyor; bu önce-FAIL/sonra-PASS kanıtı değildir. R01/R03 için run-007, R02'nin kalan hataları için run-008 production bytes'ını yalnız izole kopyada kullan; yeni test bytes'ı iki karşılaştırmada aynı olsun. Ham sonuç, beklenen assertion hatası, production/test hash'leri ayrı kaydedilsin. Eski bytes eksiksiz yeniden kurulamıyorsa tahmin etme; o karşı kanıtı INCOMPLETE yaz, kalan işleri tamamla.

   Yeni koşu envanteri kullanılan bütün değiştirilebilir kod/config/test/helper/calendar/provenance ve kanıt üreticilerini kapsasın; eksik zorunlu dosyayı sessizce atlama. Untracked dosyaları yalnız isimleriyle bağlama. Özgün stdout/stderr, JUnit ve observation'lar create-new sınırında yazılsın. Yeni çıktı manifestini bütün nihai raporlar yazıldıktan sonra üret; kendi hash'i hariç bütün teslimi, alt dizinlerdeki fixture/trace/snapshot kanıtlarını ve girdi envanterini doğrudan veya açık hash zinciriyle kapsasın. Manifest üretildikten sonra bağlanan dosya değiştirilmez. run-008'deki girdi hash'leri yeni çıktı manifestinin yerine geçmez.

   Mevcut v16 268/139 release kapısı ve eski paketler değişmez. Yeni release **önerisinde** kesin kaynak/helper/calendar/provenance payload listesi, artifact içi test envanteri, son gerçek node/toplamlar ve doğrulama sözleşmesini yaz; yalnız REVIEW_REQUIRED etiketi bırakma. `>=` ile kapıyı gevşetme, paket üretme/imzalama. Öneri review bekler.

7. **Teslim ve karar alanları.**

   Yeni koşuda okunabilir rapor, readiness, alt senaryo bazlı acceptance/observation dosyaları, hedefli/tam JUnit ve ham süreç çıktıları, compileall, tam takvim validator, continuation, production yoluna bağlı rollback harness, gerçek önce-FAIL/sonra-PASS kanıtı, girdi/önceki koşu bütünlüğü ve son çıktı manifestini teslim et. Aynı kabul hesabını raporların hepsinde kullan. Sayı artırmak hedef değildir; yukarıdaki davranışları doğrulamak hedeftir.

   | Alan | Bu turun izin verdiği sonuç |
   | --- | --- |
   | software_local_acceptance | E/R/T/C/O davranışları ve V02/V03 kanıt kapıları eksiksizse PASS; aksi FAIL/INCOMPLETE |
   | campaign_continuation | M01–M03 tam olumlu/olumsuz fixture kabulü varsa DESIGN_TESTED; aksi DESIGN_INCOMPLETE |
   | campaign_mode | CURRENT_CAMPAIGN_CONTINUATION; yeni kampanya veya reset yok |
   | source_fenced / source_snapshot / target_environment | NOT_VERIFIED |
   | broker_preflight / pending_acceptance / fill_exit / task_reboot / deployment | Gerçek ortam için NOT_RUN |
   | release_contract | REVIEW_REQUIRED; somut envanterli öneri |
   | apply_allowed / safe_to_apply / proven / parity | false |
   | overall | NO_GO |

   NO_GO nedenleri açık teknik eksikler ve henüz yapılmayan gerçek ortam doğrulamalarıdır. [Mevcut readiness reason](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run008_evidence.py:347) içindeki yalnız kârlılık/parity açıklaması yeterli değildir. `proven/parity=false` dürüstçe korunur; bunları true yapmayı teknik taşıma için yeni şart veya çalışma hedefi yapma. Pending kabulü dolum/çıkış kanıtı değildir; hiçbir yerel test gelecekte kesintisiz çalışma veya her emrin dolacağı garantisini vermez.

   Bu işi bitirince kendiliğinden operasyon aşamasına geçme. Yerel açıklar kapandıktan sonra gerçek kaynak snapshot/fencing, hedef Windows kullanıcı/MT5/task bağlamı ve broker gözlemi ayrı somut plan ve yetkiyle ele alınır. Bilinmeyen gerçek kaynak/hesap/state değerini uydurma; eksik bilgiyi ve engellediği adımı son raporda belirt. Bu tur yapılabilir diğer yerel maddeler bunun için yarım bırakılmaz.
