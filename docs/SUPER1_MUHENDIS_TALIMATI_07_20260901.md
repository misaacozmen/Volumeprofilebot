**Super1 — run-010 denetimi ve yedinci mühendis talimatı — 1 Eylül 2026**

**Karar: NO_GO.** Run-010'un byte mührü geçerlidir; semantik kabulü geçersizdir. Doğru durum: `delivery_integrity_ok=true`, R02 davranışı `PASS` fakat kanıtı `INCOMPLETE`; `software_ok=false`, `continuation_fixture_ok=false`, `evidence_integrity_ok=false`, `local_acceptance_ok=false`, `run_evidence_ok=false`. `proven=false` ve `parity=false` bilgi alanıdır; teknik taşıma kabul şartı değildir.

Bağımsız doğrulama: [run-010 manifesti](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-010/output-manifest.json:1) kendisi dışındaki 2.216/2.216 dosyayı exact relative path, byte ve SHA-256 ile bağlar; dizin toplamı manifest dahil 2.217 dosyadır. Manifest SHA-256 `762cd5a584866d5a1437433c89df5fbcac7417f953ba1cbc4d0c26989551e0bc`, bütün run-010 ağaç SHA-256 değeri `684982f3abe36385e939ca59c45f8f3bd7b808eec703f65e278b387f7fe11419` olur. Preseal 2.215/2.215 üye fiilen doğrudur. Targeted 181, full 359 JUnit case PASS; failure/error/skip sıfırdır. Run-009 50 dosya ve `129fea4fa7a6dd8639d7bfa439e0a152355c2925d9d7cc400e2c0b00f09346bc` ağaç hash'iyle değişmemiştir. Bu kazanımları koru; yanlış semantik PASS etiketlerini run-010 içinde düzeltmeye çalışma.

1. **Yetki ve teslim sınırı.**

   Run-010, `run-010.failed-observation-writer`, run-009 ve önceki koşular immutable kalır. Yalnız yerel kaynak, sentetik fixture, test, parser ve evidence runner düzelt. Gerçek kaynak/hedef state veya snapshot okuma; SSH, MT5 initialize/login, broker çağrısı/emir, scheduled task, servis durdurma, transfer, kurulum, release paketi veya gerçek imza çalıştırma. Aynı mevcut Super1 kampanyası ve aynı XM demo hesabı devam eder; başka adayla hiçbir dependency ekleme. Yeni kanıtı boş `run-011` altında, doluysa ilk sonraki boş numarada yayımla.

2. **V02/V03 — sentetik observation sistemini kaldır; gerçek davranışı ölç. Öncelik bir.**

   [Observation writer](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_observation.py:27), nodeid içinde `r02/t01/c02/remove/...` arayıp sayı ve state uyduruyor. [Aynı dosyada](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_observation.py:111) aynı sentetik sözlük expected ve actual yapılıyor; ledger/trace/snapshot da sentetik sözlükten yazılıyor. [Reporter](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_evidence_report.py:277), yalnız timestamp/`measured_by` ekleri yüzünden bunu bağımsız actual kabul ediyor. Run-010'daki 540 observation ve 1.620 bağlı artefakt hash olarak doğru, davranış kanıtı olarak geçersizdir. Örneğin [legacy ticket-conflict observation](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-010/observations/targeted/tests_test_xm_mt5_forward.py_test_r02_p_legacy_outbox_ticket_conflict_is_unsafe_without_remove_or_send--c187d3fec5e092d5.json:1) REMOVE/restart değerlerini testteki gerçek sayaçlarla çelişkili yazıyor.

   Şu sözleşmeyi uygula:

   - `_scenario(nodeid)` ve node adı/token/expected metninden actual üreten bütün kodu sil. Pytest plugin call sonucundan yalnız PASS/FAIL öğrenebilir; sayı/state/neden türetemez.
   - Expected değerleri tek merkezi, salt okunur requirement contract envanterinde node/parametre bazında tanımla. Test veya observation writer expected sözlüğünü actual olarak yeniden kullanamaz.
   - Yalnız zorunlu acceptance node'larına bir pytest fixture `record_actual(...)` ver. Test gövdesi bütün davranış assertion'larından sonra bunu **tam bir kez** çağırır. Zorunlu node hiç çağırmaz veya iki kez çağırırsa pytest session FAIL ve reporter exit nonzero olur. Zorunlu olmayan full-suite node için observation yazma.
   - Plugin her suite/node/attempt için test setup'ından önce kriptografik benzersiz `scenario_nonce` üretip fixture, stub, state/outbox, harness ve raw artefaktlara enjekte etsin. Observation içindeki `run_id/suite/nodeid/attempt_id/scenario_nonce` bütün ham kaynaklarda exact eşleşmeden recorder ve reporter FAIL etsin; başka koşudan kalan trace kabul edilmesin.
   - Fixture kurulumu tamamlandıktan, fakat davranış çağrısı başlamadan önce recorder phase checkpoint'i al. Sayaçlar yalnız checkpoint sonrası call-log/SQLite/outbox/broker delta'sından hesaplanır; setup sırasında seed edilen `.10` PENDING gibi kayıtlar acceptance gönderimi sayılmaz. Tam ham log silinmez ve observation'a bağlanır.
   - `record_actual` ham sayı kabul etmesin. Ona scenario/order kimlikleri, state root, result/finalization path'i, stub client ve gerçek trace path'leri ver. Recorder actual'i şu ham kanıtlardan çıkarsın: checkpoint sonrası stub SDK call log'undaki action bazında PENDING/REMOVE çağrıları ve request/response; harness tek metrics/call-trace kaynağı; SQLite `order_intents`, `order_event_outbox`, `broker_execution_states` readback'i; gerçek order JSONL; broker pending/history/position stub readback'i; filter/final/session/marker dosyaları. Bütün stub'lar her SDK çağrısını action, request, response/exception, `scenario_nonce` ve sıra numarasıyla kaydetsin.
   - Ledger artefaktı ilgili SQLite satırlarının ve outbox olaylarının kanonik dump'ı; trace gerçek SDK/harness call log'u; snapshot gerçek broker readback'i veya final/session/marker envanteri olsun. `{nodeid,state}` gibi üretilmiş özet artefakt kabul edilmez. Geçici kanıt pytest tmp alanı silinmeden create-new observation köküne kopyalansın.
   - Reporter ham artefaktı bağımsız okuyup PENDING/REMOVE/restart sayısını, intent/broker state'ini, kesin nedeni ve dosya hashlerini yeniden hesaplasın; observation `actual` ile ve merkezi expected contract ile üçlü eşitlik aransın. Yalnız `measured_by`, timestamp veya artefaktın kendi SHA'sının doğru olması PASS üretmez.
   - Reporter'ın izin verdiği observation seti `targeted/full × unique required node set` ile birebir olsun. Yabancı/nonrequired observation, eksik observation, duplicate suite/node veya silinmiş trace FAIL'dir. Run-010'daki bütün 181+359 node için otomatik observation üretme davranışı geri gelmez.
   - Negatifleri ayrı testler yap: observation silme; actual sayısını değiştirme; trace değiştirme; foreign run; foreign suite; foreign node/scenario; required node'da `record_actual` çağırmama; expected'i actual diye verme; nonrequired observation ekleme. Her vaka ayrı report process'inde ilgili satırı FAIL/INCOMPLETE ve exit nonzero yapar. Tek vakada hem actual hem trace bozup `any(error)` beklemek yeterli değildir.

   R02 production ve `.10/.04/.06`, SL/TP, restart, legacy/conflict davranış testlerini yeniden tasarlama; kaynak denetiminde bunlar kapanmıştır. R02 node'larının gerçek observation'larını yukarıdaki raw SDK/SQLite/broker kanıtından üret.

3. **T01 ve R03/T02 — first-known zaman nedenselliğini production'da kapat.**

   [BarStore.minute_frame](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_capital_forward.py:459) `first_known_time_utc` alanını okuyor fakat sorguda observation as-of filtresi yok. [build_frames](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_capital_forward.py:873) yalnız bar zamanını/cutoff'u kullanıyor. Sonuç olarak gelecekte öğrenilmiş satır geçmiş `market_data_asof` kararına girebilir.

   - İki zamanı ayır: `market_data_asof=cycle_started_at/requested endpoint` bar zamanı ve fetch-scope cutoff'unu belirler; `knowledge_asof=evaluation_at` fetch/ingest bittikten sonra gerçek monoton UTC saatinden alınır ve o anda bilinebilen veriyi belirler. Provider observation'ın requested `market_data_asof` sonrasında olması normaldir; onu geriye çekme ve `provider_observation_at <= market_data_asof` şartı koyma.
   - `minute_frame` için zorunlu `knowledge_asof` parametresi ekle; dönen satırların `first_known_time_utc <= knowledge_asof UTC` olmasını sorgu ve son assertion ile garanti et. Varsayılan/bypass bırakma; bütün call site'ları açık değer versin.
   - `build_frames(store, runtime, market_data_asof, knowledge_asof)` iki bacağa da aynı iki ekseni iletsin. Bar timestamp/fetch scope `market_data_asof` ile, first-known filtresi `knowledge_asof` ile kapanır. `decision_produced_at >= knowledge_asof` ve bütün saatler monoton olmalıdır.
   - `finalize_session`, `fetch_scope_end >= required_cutoff` yanında her bacakta `provider_observation_at/data_known_at <= knowledge_asof/finalization_asof` şartını arasın. Metadata eksikse `WAIT_FOR_FINAL_DATA`; `first_known_time_utc > knowledge_asof` nedeniyle elenen satır varsa exact `WAIT_FOR_KNOWLEDGE`, `excluded_future_known_count` ve `excluded_future_known_max` üret; marker/session/final yazımı sıfır. Provider'ın gerçek observation zamanını istek zamanına geri yazma.
   - [T01 testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:1045) içindeki `fresh_prefix = stale_prefix` ve 10:29'dan 10:25'e geri sarma kaldırılır. Her iki bacağın tarihsel profilini eski cutoff'a kadar seed et; ilk current-cycle fetch'te SPX tamamlayıcı segmenti eksik olsa da eski prefix **VALID**, `candidate_count=1` ve yürütülebilir tek NQ order_id içersin. İlk sonuç exact `STALE_PREFIX_NO_SEND`, checkpoint sonrası SDK PENDING=0 olur; `DATA_INVALID` veya aday sayısı sıfır bu testi geçirmez. Sonra tamamlayıcı SPX fetch'ini ingest et; monoton ileri `market_data_asof/knowledge_asof` ile **farklı path/hash ve iki-cutoff içeren yeni prefix** üret. Aynı order_id gerçek reconcile ile PENDING=1, pending readback ve SUBMITTED ledger üretir; tekrar/yeni client toplam PENDING=1 kalır.
   - [R03 testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_capital_forward.py:403) tam veriyi WAIT denemelerinden önce yüklemesin. Aynı store'da eksik iki-bacak fetch ile 10:58, 11:00:00, 11:00:07 kesin WAIT ve sıfır marker/session/final kanıtla. Ardından iki bacağın tamamlayıcı fetch'ini ingest et; gerçek provider/knowledge zamanı fetch sonrasında, zamanlar monoton ve karar zamanı knowledge zamanından sonra olacak biçimde 11:00:08 knowledge-asof ve 11:05:10 karar saatinde kesin VALID üret. Final dizini, session ve marker'ın ayrı path/byte/SHA değerlerini kaydet. Tekrar ALREADY_FINALIZED olur ve üçünün bytes/hash'i değişmez. `first_known_time_utc > knowledge_asof` satırı enjekte edilen negatif exact `WAIT_FOR_KNOWLEDGE`, excluded count/max ve sıfır artefakt üretir. Mevcut eksik ve çelişkili DATA_INVALID fixture'larını koru.

4. **C02 ve takvim negatiflerini kalıcı ve ayırt edici yap.**

   [Super1 filter yolu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_xm_mt5_forward.py:677), BLOCK/UNRESOLVED sonucunu base intent oluşturulmadan döndürüyor; neden intent/outbox'a kalıcı yazılmıyor.

   - Hem kalıcı intent/outbox'ta execution-bearing veya ekonomik kayıt olmadığını hem de broker pending/history/position readback'inde aynı comment/order_id nesnesi bulunmadığını doğrulamadan filter status yazma. Bunlardan biri varsa yeni filter intent yaratma; kontrolü base lifecycle reconciliation/cancel akışına devret.
   - Bu iki kontrol temizse BLOCK için terminal `FILTER_BLOCKED`, UNRESOLVED için yeniden doğrulanabilir `FILTER_UNRESOLVED_DEFERRED` status'lu intent ve outbox olayını tek `BEGIN IMMEDIATE` transaction içinde create-once yaz. Olay order_id, prefix path, **prefix ham dosya bytes SHA-256** değeri, cutoffs, filter state/rule/features, kesin reason ve kullanılan filter-evidence hash'ini taşır; `request_json` ve broker_ticket null kalır. BLOCK olayı `SUPER1_FILTER_TERMINAL`, UNRESOLVED olayı `SUPER1_FILTER_DEFERRED` olur.
   - Aynı order_id ve aynı evidence hash tekrarında veya **aynı kalıcı state root/SQLite üzerinde** oluşturulan yeni client tekrarında yeni outbox/SDK çağrısı oluşmaz; boş yeni store kullanma. BLOCK hiçbir zaman send'e dönmez. UNRESOLVED ancak daha yeni, tam doğrulanmış filter evidence geldiyse ve bütün cutoff/risk/global entry kapıları hâlâ açıksa atomik olarak INTENT'e alınabilir; ardından toplam tek PENDING gönderilir. Yeni evidence cutoff sonrasında gelirse terminal expiry/no-send olur. Mevcut execution-bearing veya ekonomik intent varsa filter status ile üzerine yazma; broker reconciliation/cancel sonucu üstün gelir.
   - Dört C02 parametresinin her birinde ilk, aynı-client tekrar ve new-client tekrarını ölç. Aynı kanıt altında toplam PENDING sayıları sırasıyla Monday BLOCK=0, overnight-up BLOCK=0, proven overnight-down ALLOW=1, missing previous RTH UNRESOLVED=0; REMOVE=0. BLOCK/UNRESOLVED için exact intent/outbox nedenini, ALLOW için request'in bütün alanlarını ve pending+ledger readback'ini assert et. Ayrı geçiş testinde BLOCK girdisini ALLOW gibi göstermeye çalışma SDK=0 ve mismatch/terminal kalır; UNRESOLVED'a yeni doğrulanmış previous-RTH kanıtını cutoff öncesi ekle SDK toplam=1, cutoff sonrası ekle SDK=0.
   - [Duplicate-key testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_calendar.py:121) yalnız iki `schema_version` alanlı, geri kalanı eksik JSON kullanmasın. Tam geçerli extraction bytes'ının ilk record'unda aynı değere sahip ikinci `date` anahtarı ekle; duplicate-key koruması kaldırılırsa dosya diğer bütün kontrolleri geçebilsin. Full validator kesin check adı ve `duplicate JSON key: date` nedenini vermeli. Diğer takvim mutation parametrelerini koru ve tümünü C01 REQUIRED envanterine exact nodeid ile ekle.

5. **M01–M03 PASS değildir; gerçek fixture sözleşmesini tamamla.**

   **M01:** [Yeni envanter testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:443) her reddedilen çağrıyı ayrı ölçsün. Exact/different-case source, source descendant, existing empty output, existing nonempty output ve gerçek Windows junction vakalarının **her birinden önce ve sonra** source tree ve mevcut output `relative path + bytes + SHA-256` envanteri eşit olmalı; çağrı mkdir/open'dan önce reddedilmeli. Existing-output çağrısı da bu test içinde gerçekten çalıştırılsın. Bu node ve junction node'u M01 REQUIRED listesine ekle.

   **M02:** [Mevcut fixture](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_continuation.py:73) el yazımı lock, tek release ve sabit `1*64/6*64` hash kullanıyor; kabul değildir.

   - Lock'u gerçek [`campaign_lock`](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_capital_forward.py:239) producer'ıyla, sabit TEST server env'i ve sabit saat altında üret. Çıkan bytes/şema aynen kullanılır; uydurma alan eklenmez.
   - Aynı dış TEST ONLY RSA köküyle bir genesis ve onu izleyen successor olmak üzere iki **tam kanonik** transition oluştur. Yalnız iki release fixture vardır: `R0` ve `R1`. Genesis'te `previous_transition.status=GENESIS`, `previous_transition_hash=null`, `synthetic_genesis=true`, `hashes.old.release=hashes.new.release=SHA256(raw R0 manifest)` ve release binding `R0` olur; bu anchor değişiklik yapmaz. Successor'da `hashes.old.release=SHA256(raw R0 manifest)`, `hashes.new.release=SHA256(raw R1 manifest)`, release binding `R1` olur ve predecessor'ın genesis bytes/hash/signature'ını doğrular. `genesis.new == successor.old` release/runtime/harness/contract/calendar alanlarında birebir olur. `GENESIS` yalnız bu TEST fixture'da trusted TEST root ve `synthetic_genesis=true` ile kabul edilir; gerçek source/snapshot zincirinde GENESIS kesin `UNRESOLVED`/FAIL'dir.
   - `R0` ve `R1` için ayrı `.manifest.json` + ayrı ZIP üret. Her manifest mevcut builder'ın gerçek schema/provenance alanlarını; birbirinden farklı release_id/archive; 40-hex sentetik git commit; `git_dirty=false`; Python 3.11 sürümü; exact eski v16 beş artifact dosyası; `pytest_passed_count=268` ve `artifact_pytest_count=139` değerlerini taşıyan TEST fixture olsun. Her ZIP'in üyeleri manifestle 1:1, normalize, duplicate/traversal/credential içermeyen yapıda olsun. Release hashleri ilgili manifestin imzalanan ham bytes SHA-256 değeridir; literal tekrar karakterleri yasaktır. Manifest imzası aynı TEST RSA köküyle ham bytes üzerinde doğrulanır; production trust root değiştirilmez.
   - İmzaları yeniden üreterek semantiği ayrı boz: account/server/lock; predecessor old-new kopması; başka release manifest SHA; archive/member eksikliği veya değişimi; duplicate/non-normalized/traversal/forbidden member; snapshot member kaybı; frozen/risk değişimi; eksik alan; yanlış trust root; tekrar halka; structural cycle. Her biri exact validator error code verir. Bu negatif node'ların tamamını M02 REQUIRED listesine ekle.

   **M03:** [Continuation terminal hesabı](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_continuation.py:825), runtime [`_terminal_r_state`](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_xm_mt5_forward.py:486) kurallarını paylaşmıyor; magic/symbol/duplicate-position kontrollerini atlıyor ve sidecar `raw_r` değerine güveniyor.

   - Terminal-R algoritmasını tek saf fonksiyona çıkar; runtime `_terminal_r_state` ve continuation source/snapshot validator aynı fonksiyonu kullansın. Fonksiyon magic, Super1 sembol/reward eşlemesi, OUT entry, SL/TP reason, açık position dışlama, position başına tek terminal deal, `(time_msc,ticket)` sırası, lookback/sum/scale kurallarını aynen uygular; sidecar `raw_r` alanını güven kaynağı yapmaz.
   - Manifest-bound source ve snapshot `broker-history.json/positions.json` sidecar'larını bağımsız oku ve saf fonksiyonu iki kez çağır. Exact terminal_count, son-10 liste, sum, scale ve kimlikler hem beklenen değere hem birbirine eşit olsun. Validator'a ayrı, trusted TEST root ile doğrulanmış transition/campaign-root binding içinden `expected_lock_sha256`, `expected_account_login`, `expected_server`, `expected_magic` ver; source ve snapshot'ın ikisini de bu dış otoriteye bağla. İki taraf birlikte yabancı fakat kendi içinde tutarlı kimliğe çevrilse bile exact `campaign_root_binding.semantic` hatası alınsın. Yanlış magic/symbol, duplicate terminal position, sidecar silme/değiştirme ve açık-position değişimi ayrı exact negatiflerdir.
   - Hem bar DB hem order DB için `wal_autocheckpoint=0` altında writer açıkken checkpoint edilmemiş son committed kayıt oluştur: bar satırı; order_intent; outbox event/ack/sequence; broker_execution_state. SQLite backup hepsini içersin. WAL'sız ham bar DB kopyası exact `minute_bars_and_conflicts.wal_continuity`, WAL'sız ham order DB kopyası exact `order_store.wal_continuity` koduyla reddedilsin.
   - Semantik negatifleri ayrı kopyalarda ve yeniden hesaplanmış snapshot manifest hashleriyle yap: deal silme/değiştirme; outbox sequence, ack ve event content; SUBMITTED, UNKNOWN, CANCEL_ARMED state kaybı; zorunlu dosya/alan; tutarlı başka lock/account/server. Yalnız genel `status=FAIL` assertion'ı yetmez; yukarıdaki tek exact error code aranır. WAL, sidecar ve bütün mutation node'larını M03 REQUIRED listesine ekle.

6. **O01 — production catch dead branch'ini kaldır; harness aynı policy'yi çalıştırsın.**

   [Production catch](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:625) önce latch yazıyor; sonra [pre-broker restore](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:691) ve restart dallarını `-not $persistentLatch` şartına bağlıyor. Bu dallar ulaşılamaz. [Harness](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_rollback_harness.ps1:53) bütün vakaları post-start gibi çalıştırıyor ve [sonuçta](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_rollback_harness.ps1:126) gerçek metrics yerine hep sıfır kalan değişkenleri döndürüyor.

   - `super1_rollover_failure.ps1` içine tek `Invoke-Super1RolloverCatchPolicy` koy; zorunlu `BrokerSideEffectPossible`, `TasksStopped` ve Stop/Restore/VerifyOldHashes/RestartOldTasks/VerifyOldHealth/WriteLatch/WriteEvidence/WriteHealth callback'lerini alsın. Production catch karar algoritması kopyalamadan bu fonksiyonu bir kez çağırır. Girişte var olan fatal veya recovery latch kaydedilir, asla temizlenmez ve safe rollback'i kesin yasaklar; bu durumda sıra `Stop -> WriteLatch -> WriteEvidence -> WriteHealth`, restore/restart=0 ve NO_GO'dur.
   - `BrokerSideEffectPossible=false, TasksStopped=false` ve bu attempt hiçbir app/state/task mutation'ı yapmadıysa restore/restart çağırma. Exact eski hashler, değişmeyen task XML ve fresh old health doğrulanırsa `safe_rollback=true`; mutation veya kanıt eksiğinde sıra `Stop -> WriteLatch -> WriteEvidence -> WriteHealth`, restart=0 ve NO_GO olur. Her callback öncekinin hata/null sonucuna rağmen exact bir kez denenir.
   - `BrokerSideEffectPossible=false, TasksStopped=true`: post-start latch'i baştan yazma. Staged app/state/candidate'ı exact eski hashlerine restore et; değişmeyen eski task XML ile önce ana, sonra watchdog görevini başlat ve fresh old health'i doğrula. Bütün adımlar PASS ise bu attempt fatal/recovery latch yazmamış olmalı ve `safe_rollback=true`. Watchdog start veya health düşerse iki görevi yeniden best-effort stop et, latch/evidence/critical-health yoluna geç ve bir daha restart etme. Stop/restore/hash/taskXML/restart/health kanıtlarından biri eksikse aynı unsafe yol ve NO_GO uygulanır.
   - `WriteLatch` callback'i fatal ve recovery dosyalarını birbirinden bağımsız create-new denesin ve `{latch_written,recovery_written,paths,errors}` döndürsün. Null dönüş, ikisinin de yazılamaması ve yalnız fatal yazılıp recovery'nin yazılamaması ayrı ölçülür; hiçbirinde sonraki callback atlanmaz.
   - `BrokerSideEffectPossible=true` için sıra değişmez: `WriteLatch` dene ve hataları tut; `Stop` her durumda dene; sonra `WriteEvidence`; sonra `WriteHealth`. Old/new ekonomik state ve arşiv korunur; restore=0, restart=0. Latch null/failed/partial olsa bile stop/evidence/critical-health çalışır; ayrıntı errors/evidence içinde görünür ve NO_GO kalır.
   - Harness aynı production policy fonksiyonunu şu exact vakalarla çağırır: pre-boundary tasks-running/no-mutation safe no-op; pre-boundary tasks-stopped safe restore/restart; preexisting latch; pre-boundary restore failure; post start-timeout; watchdog failure; stop failure; gerçek production evidence-writer I/O failure; iki latch yazımının da başarısızlığı; yalnız fatal yazılan partial-latch. Scriptblock içinde yalnız `throw` ile evidence hatası üretme; gerçek writer'a erişilemez dosya/dizin hedefi ver.
   - Tek metrics nesnesi start_initial, stop_failure, restore, restart_after_failure ve broker_new_entry sayaçlarını beslesin; JSON, stdout ve call-trace aynı değeri görsün. Tasks-running safe no-op vakasında restart_after_failure=0; tasks-stopped safe restore vakasında RestartOldTasks çağrısı=1; ikisinde de broker new-entry=0. Bütün unsafe/post vakalarda restart_after_failure=0 ve broker=0; post vakalarda ilk start girişimi dürüstçe 1 olur.
   - Her vaka için source/app/state/task XML ve ekonomik dosyaların önce/sonra envanterini kaydet; yalnız policy'nin izin verdiği farkları exact assert et. Yeni Python process aktif latch'i gerçek state root'ta tüketir. Boş başka dizin kontrolü bağlantı negatifi sayılmaz.
   - Production/helper/harness için PowerShell parser PASS ve AST kontratı ekle. AST production'ın policy'yi exact parametrelerle bir kez çağırdığını doğrulasın; policy çağrısı kaldırılmış production kopyası FAIL olsun. String içinde `$brokerSideEffectPossible` geçmesi O01 kanıtı değildir.

7. **Evidence runner, verifier, envanter ve karar alanlarını fail-closed yap.**

   - [Runner input envanteri](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_run010_evidence.py:78), git hata kodunu yok sayıyor ve ignored zorunlu dosyaları görmüyor. Git process exit nonzero ise dur. Tracked deletion veya zorunlu dosya eksikliği FAIL olsun; `if not path.is_file(): continue` yok. Explicit mandatory listeye en az `live_forward/capital_demo_config.json`, `live_forward/xm_mt5_demo_config.json`, `live_forward/super1_xm_mt5_demo_config.json`, candidate/contract/baseline lock, bütün runtime/deploy/helper/calendar/provenance/release-integrity/builder dosyaları, yeni test/observation/verifier/runner dosyalarını ekle. Ignored olması dışlama nedeni değildir. Before/after 1:1 eşitlik aranır.
   - `required_mapping_count==53` ve `unique_required_node_count==47` sabit kapısını kaldır. Count'ları merkezi REQUIRED setinden dinamik hesapla; JUnit, observation, matrix, proposal ve verifier exact set/count üzerinde anlaşsın. Yeni zorunlu node'larla değer doğal olarak değişir; eski sayı PASS şartı değildir.
   - Sealed dosyalarda yalnız sonuçları dondurmadan önce bilinen dört **claim** kapısını yaz: `software_claim_ok` production davranışları, targeted/full testler, compileall, takvim, PowerShell parser/AST ve ortak O01 policy+harness sonuçlarının AND'idir; `continuation_fixture_claim_ok` M01–M03 positive/negative sonuçlarının AND'idir; `evidence_payload_claim_ok` process metadata/exit'ler, exact JUnit setleri, nonce-bound raw observationlar, reporter, input/predecessor/baseline ve preseal içeriğinin AND'idir; `local_acceptance_claim_ok=software_claim_ok && continuation_fixture_claim_ok && evidence_payload_claim_ok`. Henüz çalışmamış pre-publish/final verifier sonucunu bu sealed claim'lere katma. O01'i continuation claim'ine, observation negatiflerini software claim'ine taşıma.
   - `preseal_integrity_ok=true` sabit yazma. Bütün candidate payload ve preseal inventory yazıldıktan sonra hiçbir candidate byte'ı değiştirme. Ayrı OS process olarak çalışan `verify_super1_evidence_run.py --phase prepublish` geçici dizini salt okunur doğrulasın; preseal kapsamını, cross-report kapılarını, process/JUnit/raw-observation/input/predecessor/baseline bağlarını yeniden hesaplayıp yalnız stdout/exit döndürsün, temp run içine dosya yazmasın. PASS olmadan final output manifesti ve rename yok; FAIL'de temp dizini `.failed-<attempt_id>` yapıp nonzero çık.
   - Pre-publish PASS sonrasında final output manifestini create-new ve son üye olarak yaz; sonra atomik rename yap. Aynı ayrı verifier final modunda manifestin 1:1 path/byte/SHA kapsamını, semantik kontrolleri, canonical basename=`run_id` eşleşmesini ve göreli referansları baştan doğrulasın; `.failed/.tmp` yol kesin reddedilir. Final verifier sealed run içine hiçbir şey yazmaz ve final sonucu sealed claim alanlarına geri beslenmez.
   - Sealed acceptance/readiness/evidence/process/manifest dosyalarında `publication_status=SEALED_CANDIDATE`, `authoritative=false` ve `authoritative_if={canonical_basename_exact:true,matching_external_receipt_required:true,final_verifier_pass_required:true}` bulunur. Final verifier PASS sonrası run dışında create-new `<actual_run_id>.verification.json` receipt'i `publication_status=PUBLISHED`, `authoritative=true`, final path, manifest SHA, verifier source SHA, verified member count, `prepublish_verifier_ok=true`, `final_semantic_verifier_ok=true` ve `delivery_integrity_ok=true` taşır. Receipt `software_candidate_ok=software_claim_ok && final_semantic_verifier_ok`, `continuation_fixture_candidate_ok=continuation_fixture_claim_ok && final_semantic_verifier_ok`, `evidence_payload_candidate_ok=evidence_payload_claim_ok && prepublish_verifier_ok && final_semantic_verifier_ok`, `local_acceptance_candidate_ok` ise bu üçünün AND'i olarak yeniden hesaplar. `local_acceptance_ok=local_acceptance_candidate_ok`; `run_evidence_ok=local_acceptance_candidate_ok && delivery_integrity_ok` olur. Receipt `run_id`, manifest `run_id` ve canonical final basename exact eşleşir; sealed bytes bu sonuçlarla sonradan değiştirilmez.
   - Her attempt için benzersiz `attempt_id` kullan; attempt başında canonical final dizin ve canonical receipt ikisi de yoksa başla. Final verify hata verirse canonical dizini `.failed-<attempt_id>` adına geri çek ve exit nonzero yap. Final verify sonrasında receipt'i create-new handle ile yaz, flush et ve kapat. Collision/I/O/partial-write hatasında canonical dizini yine `.failed-<attempt_id>` adına çek; yalnız bu attempt'ın oluşturduğu partial receipt'i canonical olmayan `.failed-<attempt_id>` adına taşı, collision yaratan yabancı dosyaya dokunma ve exit nonzero yap. `.tmp`/`.failed` candidate bytes'ını yeniden yazma; bunlar path ve eşleşen receipt yokluğu nedeniyle hiçbir zaman authoritative değildir.
   - Sealed JSON içinde silinmiş `.<actual_run_id>.tmp-*` mutlak path bırakma. Bütün JUnit/observation/artifact referansları final run'a göre relative ve mevcut olmalı; verifier absolute temp/workspace path bulursa FAIL etsin.
   - Run-010 çıktıları kaynak bytes'ını içermez; yalnız input path/hash envanteri içerir. Düzenleme öncesi baseline'a alınacak her mevcut kaynak dosyasını bu envanterdeki path/byte/SHA ile exact doğrula, sonra salt okunur kopyala. Tek mismatch/eksik varsa tarihsel baseline kurulmuş sayma ve fail-closed dur; hash'ten kaynak üretme. Run-010 envanterinde olmayan ignored `live_forward/capital_demo_config.json`, `live_forward/xm_mt5_demo_config.json` ve `live_forward/super1_xm_mt5_demo_config.json` için ayrı `RUN010_UNBOUND` yaz, tarihsel eşitlik veya before-FAIL iddiası kurma; üçünü de yeni koşunun zorunlu input envanterine dahil et. Aynı yeni regresyon test bytes'ını doğrulanabilen baseline ve current kaynak üzerinde çalıştır: observation, temporal causality/T01, C02 persistence, M01–M03 ve O01 eski bytes'ta hedeflenen nedenle FAIL, yeni bytes'ta PASS. Baseline/current source SHA, test SHA, command, exit ve ham stdout/stderr manifest-bound olsun. Çalışan ağacı checkout/restore etme.

8. **Release önerisi ve readiness doğru şeyi söylesin.**

   [Run-010 proposal](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-010/continuation-contract-proposal.json:1) on dosyalık eksik payload veriyor; gerçek builder'ın [base+Super1 required payload](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/build_signed_windows_release.ps1:230) kapanışını kapsamıyor. Proposal'da paket üretmeden iki ayrı envanter oluştur. `required_payload_closure`, builder'ın bütün base+Super1 required payload/provenance dosyaları + bu turda değişen runtime/deploy/helper/config/candidate dosyaları + local import/dot-source/config-reference bağımlılıklarının deterministik union'udur. `prospective_archive_member_inventory`, builder'ın staging copy ve exclusion kurallarını salt okunur uygulayarak ZIP üretmeden arşive girecek **bütün** üyeleri listeler; state'i `PROSPECTIVE_NOT_BUILT` olur. Her ikisinde path/bytes/SHA ve inclusion/rule reason bulunur; required closure prospective envanterin alt kümesi değilse veya herhangi dependency eksikse proposal FAIL'dir. Bu envanter gerçek ZIP üye kanıtı diye etiketlenmez.

   Eski v16 schema/profile, exact beş artifact dosyası ve 268/139 kapısı değişmez; `unchanged=true` hardcode yazma, mevcut eski release/manifest/signature bytes/hash kanıtı yoksa `NOT_VERIFIED` yaz. Successor için exact yeni artifact node/file envanteri ve son targeted/full exact toplamları ayrı önerilir. Mevcut [release integrity](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/release_integrity.ps1:16) successor'ın ölçülmüş genişletilmiş artifact listesini kabul etmiyorsa bunu `SUCCESSOR_CONTRACT_CHANGE_REQUIRED` diye açık blocker yaz; eski profile'ı gevşetme. Status `REVIEW_REQUIRED / NOT_BUILT / NOT_SIGNED`; ZIP, paket ve imza yok.

   Readiness alanları exact şu semantiği taşır: `campaign_mode=CURRENT_SUPER1_CAMPAIGN_CONTINUATION`; fresh/new campaign etiketi yasak. Belirsiz `runtime_state=NOT_DEPLOYED` alanını kullanma: `source_runtime_state=NOT_VERIFIED`, `target_runtime_state=NOT_VERIFIED`, `target_deployment_state=NOT_RUN`, `deployment_state=NOT_RUN`, `successor_release_state=NOT_BUILT`, `successor_signature_state=NOT_SIGNED` yaz. `source_fenced/source_snapshot/target_environment=NOT_VERIFIED`; broker/pending/fill-exit/task-reboot/deployment/release gerçek ortam kapıları `NOT_RUN`; `safe_to_apply/apply_allowed/proven/parity=false`; `overall=NO_GO`. `proven/parity` teknik NO_GO nedeni yapılmaz. Yerel açık varsa ayrı listelenir; yerel aday kapıları kapanırsa NO_GO yalnız gerçek ortamın NOT_RUN/NOT_VERIFIED kapıları nedeniyle sürer.

9. **Yeni koşunun kabulü ve teslimi.**

   Bütün yeni node'lar targeted ve full JUnit'te PASS; compileall, calendar validator, PowerShell parser/AST ve gerçek ortak rollback harness exit 0 olmalı. Her REQUIRED node için iki suite'te nonce-bound gerçek raw observation bulunmalı; report tarafından yeniden hesaplanan actual exact expected olmalı. M01–M03'ün tüm pozitif/negatif node'ları olmadan sealed `continuation_fixture_claim_ok`; O01 dahil production/harness node'ları olmadan `software_claim_ok`; observation negatifleri, input/predecessor/baseline/preseal olmadan `evidence_payload_claim_ok` true olamaz. Üç sealed claim ve iki ayrı verifier sonucu dış receipt'te yeniden hesaplanmadan hiçbir candidate gate true olamaz. Canonical final path ve bağımsız final verifier olmadan `delivery_integrity_ok`; basename/run_id eşleşen dış receipt olmadan authoritative `local_acceptance_ok/run_evidence_ok` true olamaz.

   Teslim: acceptance matrix, readiness, evidence report, process metadata, targeted/full JUnit, gerçek observation+raw artefaktlar, before-FAIL/after-PASS kanıtı, M01–M03 fixture raporları, rollback case raporları, successor contract proposal, input before/after, predecessor before/after, preseal, final manifest ve dış final-verifier receipt. Sayıları koşudan ölç; sabit veya `>=` kullanma. Run-010 ve failed sibling'in before/after ağaç hashlerini ayrıca kaydet.

   Bu yerel tur gerçekten kapanmadan SSH/source/target/broker preflight'a geçme. Yeni koşu başarıyla yayımlansa dahi kendiliğinden bağlantı veya deployment başlatma; mimarın sonraki talimatını bekle.
