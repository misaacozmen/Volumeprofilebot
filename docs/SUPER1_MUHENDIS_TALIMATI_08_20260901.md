**Super1 — run-012 ret kararı ve sekizinci mühendis talimatı — 1 Eylül 2026**

**Karar: NO_GO.** Run-012'nin byte teslimi geçerlidir; semantik kabulü ve dış receipt'in `local_acceptance_ok/run_evidence_ok=true` iddiaları geçersizdir. Mimari değerlendirmede `delivery_byte_integrity_ok=true`, `software_claim_ok=false`, `continuation_fixture_claim_ok=false`, `evidence_payload_claim_ok=false`, `local_acceptance_ok=false`, `run_evidence_ok=false` kabul edilecektir. R02 production davranışı `PASS`, R02 observation kanıtı `FAIL` durumundadır. Gerçek ortam aşamasına geçme.

Bağımsız byte doğrulaması: [run-012 manifesti](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-012/output-manifest.json:1) manifest dışındaki 432/432 üyeyi exact relative path, byte ve SHA-256 ile bağlar; toplam dosya sayısı 433'tür. Manifest SHA-256 `7e38b8dbee672ff8ba1a1d966531b2b636fe384c593356b104a87498caaaa579`, run-012 bütün ağaç SHA-256 `3c7f47062439182298c120f37d6a2cda4c621c0ac90205b2035f0656bd446002`, [receipt](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-012.verification.json:1) SHA-256 `ea4956f1f56ddfc0db973249f3c592ff8d7f7c30058c0c4917d36b50fc150161` olur. Run-011 433 dosya ve `257354aa344aaffb05c6ec2d3f9065b23dfb831f8305c32020c8abb82adefe09` ağaç hash'iyle korunmuştur. [Talimat-07](C:/Users/ISAAC/Documents/otobacktestprojesi/docs/SUPER1_MUHENDIS_TALIMATI_07_20260901.md:1) SHA-256 `54613716a57eebe2aec72d54ee146395efd88f6c97b80ec376549812fd5ac2c2` ile değişmemiştir.

1. **Yetki, değişmezlik ve çalışma sırası.**

   Run-012, dış receipt'i, run-011, run-010 ve bütün önceki talimatlar immutable kalır. Run-012 receipt'ini silme, yeniden yazma veya geriye dönük `authoritative=false` yapma; yeni raporda `predecessor_byte_integrity=PASS`, `predecessor_semantic_acceptance=REJECTED` olarak referans ver. Talimat-07 yürürlüktedir; bu belge denetimde ortaya çıkan eksikleri daraltır. Çelişki halinde bu belgedeki daha kesin kural uygulanır.

   Yalnız yerel kaynak, sentetik fixture, parser, test, runner ve verifier üzerinde çalış. SSH, kaynak/hedef runtime okuma, MT5 initialize/login, broker bağlantısı veya emir, scheduled task sorgu/değişikliği, servis, transfer, kurulum, gerçek release build/production ZIP, dependency download veya production-key imzası çalıştırma. M02'nin pytest temp root'ta ephemeral TEST key ile ürettiği `TEST_ONLY_SYNTHETIC_FIXTURE` arşiv/imzası bu yasağın tek istisnasıdır; successor paketi/imzası değildir ve hiçbir `NOT_BUILT/NOT_SIGNED` alanını karşılamaz. Başka adaya dependency ekleme. Aynı mevcut Super1 kampanyası ve aynı XM demo hesabı devam eder; init/reset/rollover yoktur.

   İlk işlem olarak mevcut düzenleme-öncesi kaynakları run-012 `input-inventory-before.json` path/byte/SHA değerleriyle doğrula ve salt okunur `baseline/run-012-pre-fix` kopyasına al. Tek mismatch varsa kod değiştirmeden dur ve baseline'ı `INVALID` yaz. Run-012 envanterinde bulunmayan gerçek `live_forward/xm_mt5_demo_config.json` için `RUN012_UNBOUND_CURRENT_PRE_EDIT_ONLY`; run-010'da bağlı olmayan üç config için `RUN010_UNBOUND` yaz. Hash'ten dosya üretme, git checkout/restore yapma ve tarihsel bytes iddiası uydurma. Yeni regression testi tek sealed test bytes'ı olarak baseline dışında tutulur ve harici harness tarafından iki kez çalıştırılır; yalnız source/import root baseline ile current arasında değişir. Test SHA'sı ve argv exact aynıdır, source-root SHA'ları farklıdır; baseline read-only kalır ve içine yeni test kopyalanmaz. Baseline hedeflenen exact nedenle FAIL, current PASS olmalıdır.

2. **V02/V03 — otomatik/sentetik observation yolunu tamamen kaldır.**

   [Autouse adapter](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/conftest.py:18) test gövdesi ölçüm yapmadığında sonradan boş `sdk_calls`, sıfır sayaçlar ve `N/A/PASS` state üretmektedir. [Writer](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_observation.py:97) bu trace içindeki self-claimed `actual` sözlüğünü kendi hesabının üzerine yazmakta; [reporter](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/super1_evidence_report.py:274) `N/A` sayaçlarını ve state/expected eşitliğini atlamaktadır. Run-012'de 94/94 observation'ın SDK logu boş, bütün üç sayaç sıfır, bütün state alanları `N/A`; 282 raw dosya yalnız bu sentetik sözlükleri bağlar. Buna rağmen PASS üretilmiştir.

   Şu API ve sözleşmeyi aynen uygula:

   - `tests/conftest.py::_record_required_node_after_test` ve test çağırmadığında observation üreten bütün fallback/autouse kodunu sil. Dosyada başka fixture kalmıyorsa dosyayı sil. Hiçbir plugin veya teardown test davranışından bağımsız actual üretemez.
   - Merkezi contract iki set içersin: `B = BEHAVIOR_REQUIRED_NODES` ve `N = EVIDENCE_NEGATIVE_REQUIRED_NODES`; `B ∩ N = ∅`. Targeted ve full JUnit'in her birinde `nodeids(JUnit) ∩ (B ∪ N) = B ∪ N` olur ve her required node o suite'te exact bir kez PASS eder; nonrequired testler bu sayımlara girmez. Her suite'in observation node seti exact `B` olur; `N` için observation sayısı sıfırdır. `required_mapping_count` requirement→node edge sayısı, `unique_required_node_count=|B ∪ N|`, `behavior_observation_node_count_per_suite=|B|`, toplam observation sayısı `2|B|` olarak artefaktlardan dinamik hesaplanır.
   - Setlerin tek normatif otoritesi architect-owned [V08 required-node manifestidir](C:/Users/ISAAC/Documents/otobacktestprojesi/docs/SUPER1_REQUIRED_NODE_MANIFEST_V08_20260901.json:1); SHA-256 exact `d58ec29ba93cdbc88a9978e1860bf8ae5eaf8fa5913778a6de1b5d1ff2c3fbdf`. Bu dosyayı değiştirme. İçindeki run-012 53-edge/47-node tabanı ile V08 delta birlikte zorunludur: `|B|=164`, `|N|=53`, `|B∪N|=217`, mapping edge `225`, suite başına behavior observation `164`, iki suite toplamı `328`. Sayılar threshold değildir; verifier manifest bytes/hash, edge listesi ve iki setten bunları yeniden hesaplayıp exact eşitlik arar. Producer/reporter/test koleksiyonu beklenen seti kendi kodundan tanımlayamaz; manifest mandatory input olur, before/after SHA ile bağlanır ve sealed teslimde exact kopyası bulunur. Parametrik node'lar manifestteki explicit `ids=[...]` değerlerini kullanır; pytest default value/error-message ID'si veya node rename FAIL'dir.
   - Exact API `actual_evidence.checkpoint() -> opaque CheckpointToken` ve `record_actual(*, checkpoint: CheckpointToken) -> None` olur. Fixture kurulumu ve seed tamamlandıktan, test edilen çağrıdan hemen önce checkpoint exact bir kez; bütün assertion'lardan sonra record exact bir kez çağrılır. Token tek kullanımlı ve `run_id/suite/nodeid/attempt_id` bağlıdır. Test expected, actual, count/state, raw dict/path/sequence veya adapter seçimi veremez. Kaynaklar plugin-owned typed adapter registry'den gelir; merkezi node contract her node için exact `required_raw_sources` şemasını taşır. Missing veya extra kaynak FAIL'dir. Eksik/fazla checkpoint ya da record pytest/plugin session sürecini nonzero yapar.
   - Runner attempt başında tek `attempt_id` üretir. Plugin her suite/node/attempt için kriptografik benzersiz `scenario_nonce` ve `checkpoint_id` üretir. Observation ve her raw envelope exact `run_id`, `suite`, `nodeid`, `attempt_id`, `scenario_nonce`, `checkpoint_id` taşır. Reporter altı alanın tamamında exact eşitlik arar. Test-only kimlikleri production SQLite schema'sına ekleme; SQLite/outbox/broker dump'larını bu kimlikleri taşıyan raw envelope içine koy.
   - Checkpoint SDK sequence; SQLite için ilgili tabloların canonical primary-key + before-row/hash envanteri, max sequence ve DB/WAL continuity; order JSONL byte offset/hash; broker-stub call sequence ve ilgili dosya envanterini kaydeder. Record aynı kaynakların after-row/hash durumunu alır; created/updated/deleted delta bu iki immutable görünümden hesaplanır. Yalnız row count/max-id karşılaştırması kabul değildir. Actual yalnız checkpoint sonrası delta'dan hesaplanır. Setup'ta seed edilen `.10` PENDING, eski outbox veya eski broker nesnesi acceptance çağrısı sayılmaz; tam log yine korunur.
   - `record_actual` API'sinden `expected`, sayaç ve state parametrelerini kaldır. Expected yalnız salt okunur merkezi node/param contract'ından gelir. Her sayaç sayısaldır; kullanılmayan işlem için `0` yazılır. `N/A`, wildcard, prose `CONTRACT` veya test tarafından verilen expected PASS yasaktır.
   - Trace schema'sından `actual` alanını kaldır; bulunursa recorder ve reporter exact `TRACE_SELF_CLAIMED_ACTUAL` ile FAIL etsin. Writer yalnız raw kaynakları bağlayıp kendi ölçümünü yazar. Reporter writer'ın `_measure_raw` veya başka ölçüm fonksiyonunu import etmez; raw SDK, SQLite, outbox, broker ve dosya bytes'ını bağımsız parser ile yeniden hesaplar.
   - Evrensel ve ilgisiz bir raw-source listesi kullanma. Merkezi contract execution node'ları için instrumented SDK + intent/outbox/execution SQLite + order JSONL + broker pending/history-order/history-deal/position; finalization için fetch/store + final tree/session/marker; filter için raw prefix/evidence + lifecycle kaynakları; rollback için call trace/metrics/before-after inventory; calendar ile M01–M03 için kendi typed parser/inventory kaynak setlerini exact tanımlar. İlgili kaynak kullanılmış fakat işlem oluşmamışsa instrumented kaynaktan gerçek boş delta gelir ve expected sayı `0` olur.
   - Deterministik semantic payload ve semantic hash alanlarında merkezi expected, recorder actual ve bağımsız recomputation exact üçlü eşit olur. `attempt_id/scenario_nonce/checkpoint_id` taşıyan dinamik transport/envelope bytes SHA'sı merkezi sabitle karşılaştırılmaz; sealed actual bytes, bağımsız recomputation ve manifest/reference arasında exact doğrulanır. Merkezi contract ancak typed `sha256_of(source_name)` ifadesi kullanabilir; wildcard/prose yasaktır. State alanının yalnız boş olmaması PASS değildir. `actual == recomputed` kopyası kanıt değildir.
   - R02 davranış kodunu değiştirme. Özellikle `test_r02_d_cancel_control_resolves_remainder_before_economic_exit` gerçek checkpoint sonrası REMOVE=1; T01 ve C02 ALLOW gerçek PENDING=1 göstermelidir. Run-012'deki sıfır trace bu node'larda yeniden kullanılamaz.

   `tests/test_super1_observation.py` içinde aşağıdaki node'ları ayrı subprocess/mutation vakaları olarak exact adlarla ekle. Eksik/fazla checkpoint veya record pytest/plugin session sürecini; nonrequired observation ile test-supplied expected/measurement recorder'ı; sealed/raw mutation reporter veya verifier'ı nonzero yapar. Her node tek exact error code assert eder. Merkezi validation-precedence tablosu aynı anda birden çok bozukluk olsa bile hangi tek kodun döneceğini belirler:

   - `test_v02_required_node_without_record_actual_fails_session`
   - `test_v02_required_node_double_record_actual_fails_session`
   - `test_v02_required_node_without_checkpoint_fails_session`
   - `test_v02_required_node_double_checkpoint_fails_session`
   - `test_v02_nonrequired_node_observation_is_rejected`
   - `test_v02_foreign_run_id_is_rejected`
   - `test_v02_foreign_suite_is_rejected`
   - `test_v02_foreign_nodeid_is_rejected`
   - `test_v02_foreign_scenario_nonce_is_rejected`
   - `test_v02_foreign_attempt_is_rejected`
   - `test_v02_foreign_checkpoint_is_rejected`
   - `test_v03_trace_actual_override_is_rejected`
   - `test_v03_actual_tamper_is_rejected`
   - `test_v03_trace_tamper_is_rejected`
   - `test_v03_ledger_tamper_is_rejected`
   - `test_v03_snapshot_tamper_is_rejected`
   - `test_v03_missing_raw_source_is_rejected`
   - `test_v03_stale_raw_source_from_other_attempt_is_rejected`
   - `test_v03_test_supplied_expected_is_rejected`
   - `test_v03_test_supplied_measurement_is_rejected`

   Mevcut birleşik `actual+trace tamper`/`any(error)` testi kabul değildir. Bu node'ları V02/V03 mapping'ine ve `EVIDENCE_NEGATIVE_REQUIRED_NODES` setine ekle.

3. **T01, R03/T02 ve zaman nedenselliğini gerçekten kapat.**

   Production'daki SQL first-known filtresi ve ayrı `market_data_asof/knowledge_asof` parametreleri doğru başlangıçtır; aşağıdaki eksikleri düzelt:

   - `minute_frame` dönüş assertion'ı `max(known_time) <= knowledge_asof UTC` arar. Parametre zorunludur; bypass/default yoktur.
   - `run_prefix(..., market_data_asof, knowledge_asof)` iki zamanı zorunlu alır. `knowledge_asof or utc_now()` kaldırılır. `run_once`, fetch'lerden sonra aldığı `evaluation_now` değerini açıkça geçirir. Prefix record `market_data_asof`, `knowledge_asof`, `decision_produced_at` taşır ve prefix üretilirken yalnız `market_data_asof (= cycle_started_at) <= knowledge_asof <= decision_produced_at` monotonluğunu doğrular. Gelecekte oluşacak `send_guard_at` prefix'e yazılmaz ve prefix sonradan değiştirilmez. Reconcile/send guard alındıktan sonra `decision_produced_at <= send_guard_at` ayrıca doğrulanır ve sonuç execution/outbox observation'ına yazılır. Provider observation'ın requested market as-of'tan sonra olması geçerlidir; `provider_observation_at <= market_data_asof` şartı koyma.
   - İmza exact `finalize_session(..., *, market_data_asof, knowledge_asof, finalization_asof, fetches)` olur; bu parametrelerin default'u ve positional geçişi yoktur. Her iki leg için `fetch_scope_end`, `provider_observation_at` ve scope'a ait `data_known_at` zorunludur. `market_data_asof <= provider_observation_at/data_known_at <= knowledge_asof <= finalization_asof <= decision_produced_at` monotonluğu aranır. Metadata eksikse exact `WAIT_FOR_FINAL_DATA`; provider/data-known knowledge zamanından ilerideyse exact `WAIT_FOR_KNOWLEDGE`, count/max ve leg dağılımı; hiçbir final/session/marker yazımı yoktur.

   [T01 testini](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_xm_forward.py:1045) şu tek senaryoyla değiştir:

   - İki leg'in tarihsel profilini eski cutoff'a kadar seed et. İlk current-cycle fetch yalnız NQ tamamlayıcı segmentini getirir; SPX yeni segmenti eksik kalır. `market_data_asof=2026-08-05 10:25:00 America/New_York`, `knowledge_asof=10:25:10` altında eski prefix `VALID`, `candidate_count=1` ve yürütülebilir tek NQ order_id içerir.
   - Eski prefix'i `10:29:00` send guard ile reconcile et: exact `STALE_PREFIX_NO_SEND`, checkpoint sonrası PENDING=0, REMOVE=0 ve `PRE_SEND_DEFERRED` readback.
   - `10:29:05` sonrasında iki leg için tamamlayıcı fetch ingest et: NQ scope `[10:25:00,10:29:10)`, SPX scope kendi eksik başlangıcından `10:29:10`'a kadardır; iki provider observation zamanı da `knowledge_asof=10:29:20` değerini aşmaz. Yeni `market_data_asof=10:29:10`, send guard `10:29:30` ile yeni prefix üret. Exact cutoff vector `{nq:10:27, spx:10:25}` olur. Path ve raw bytes SHA eski prefix'ten farklıdır; aynı order_id kullanılır. Exact PENDING=1, SUBMITTED intent/outbox ve pending broker readback üret; aynı-state yeni client tekrarı toplam PENDING=1 bırakır.
   - `fresh_prefix = stale_prefix`, zaman geri sarma, `DATA_INVALID` veya candidate_count=0 bu node'u FAIL yapar.

   R03/T02 testini şu progressive fixture ile değiştir:

   - Aynı store'a başlangıçta final pencereyi tamamlamayan gerçek iki-leg fetch'leri yükle. 10:58, 11:00:00 ve 11:00:07 denemeleri actual eksik scope/data nedeniyle exact `WAIT_FOR_FINAL_DATA`; final directory/session/marker yoktur. Tam veriyi bu denemelerden önce yükleme.
   - Requested `market_data_asof=11:00:08`, provider observation `11:00:08.500`, `knowledge_asof=11:00:09`, karar/finalization `11:05:10` olacak tamamlayıcı iki-leg fetch'i aynı store'a ingest et; exact VALID üret.
   - Final attempt directory için recursive, relative path'e göre sıralı üye envanteri (`relative path + file bytes + file SHA-256`) ve bundan canonical tree SHA kaydet. `session.json` ile marker için ayrıca doğrudan relative path/bytes/SHA kaydet. Tekrarda ALREADY_FINALIZED olur; directory tree, session ve marker kanıtlarının tamamı byte-exact değişmez.
   - Ayrı negatifte required bar'ın `first_known_time_utc=11:00:10`, knowledge `11:00:09` olsun. Exact `WAIT_FOR_KNOWLEDGE`, excluded count=1, exact max ve sıfır final/session/marker assert et. Eksik provider/data-known metadata için ayrı `WAIT_FOR_FINAL_DATA` node'u ekle.

   T01 progressive, R03 progressive, future-known ve missing-metadata node'larını ilgili mapping'lere `BEHAVIOR_REQUIRED_NODES` olarak ekle.

4. **C02 ve C01 negatiflerini tamamla.**

   [C02 persist yolu](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_xm_mt5_forward.py:640) için şu exact kuralları uygula:

   - Clean gate aynı order_id/comment için `order_intents`, `broker_execution_states`, ekonomik/execution-bearing `order_event_outbox`, broker current orders, positions, history orders ve history deals kaynaklarının tamamını okur. Mevcut intent `FILTER_BLOCKED` ise idempotent blocked kalır; `FILTER_UNRESOLVED_DEFERRED` ise aynı/eski evidence altında idempotent unresolved kalır, yalnız aşağıdaki newer promotion/expiry kuralı uygulanabilir. Başka bir intent durumu veya broker/execution/non-filter ekonomik outbox/broker nesnesi varsa yeni FILTER intent/outbox yazmadan base lifecycle'a exact `FILTER_DEFERRED_TO_BASE_LIFECYCLE` ile devret. Her kaynak ve dal için ayrı negatif test yap.
   - Prefix hash parsed/canonical JSON'dan değil prefix path'teki ham dosya bytes'ından hesaplanır. Olay relative prefix path, raw bytes/SHA, cutoffs, full filter state/rule/features, exact reason, evidence `observed_at`, monoton `evidence_revision` ve raw filter-evidence SHA taşır. `schema_version` yalnız belge formatıdır ve evidence yeniliği sıralamasında kullanılmaz.
   - BLOCK terminal `FILTER_BLOCKED` kalır ve hiçbir yeni evidence ile send'e dönmez. UNRESOLVED `FILTER_UNRESOLVED_DEFERRED` kalır. Aynı/eskimiş evidence yeni event/SDK üretmez.
   - UNRESOLVED promotion için evidence ancak `evidence_revision` daha büyük, `observed_at` daha geç, cutoff vector componentwise nondecreasing ve raw SHA farklıysa strictly newer sayılır; dört koşulun tamamı zorunludur. Açık cutoff, risk ve global-entry kapıları altında tek `BEGIN IMMEDIATE` transaction içinde CAS ile `FILTER_UNRESOLVED_DEFERRED -> PRE_SEND_DEFERRED` geçişi ve create-once promotion event'i üret. Transaction sonrasında base lifecycle bütün kapıları yeniden okur ve atomik olarak `INTENT`/send akışına devam ederek toplam tek PENDING dener. Cutoff sonrası evidence exact kalıcı `FILTER_EXPIRED_NO_SEND` durumu ve create-once `SUPER1_FILTER_EXPIRED` olayı üretir; asla send'e dönmez.
   - Dört C02 parametresinin her birinde aynı persistent state root üzerinde first call, same-client repeat ve new-client repeat çalıştır. BLOCK/UNRESOLVED exact intent/outbox/readback ve toplam PENDING=0/REMOVE=0; ALLOW toplam PENDING=1 ve full request+pending+ledger assert eder. BLOCK mismatch, UNRESOLVED newer-before-cutoff, newer-after-cutoff ve her clean-gate kaynağı ayrı REQUIRED node olur.

   [Takvim duplicate testi](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/tests/test_super1_calendar.py:121) eksik `{"schema_version":...}` belgesini kullanmayacak. Mevcut tam geçerli Nasdaq extraction bytes'ının ilk `records` öğesine aynı değerli ikinci `date` anahtarı ekle; duplicate guard kaldırılırsa belge diğer bütün validation'lardan geçebilir durumda olsun. Loader/wrapper alttaki exact nedeni kaybetmeden `source_bytes_hashes` check'i ve `duplicate JSON key: date` hatasını döndürsün. `test_calendar_semantic_mutations_fail_through_full_validator` içindeki 12 parametreli node'un tamamını V08 manifestindeki explicit ID'lerle C01 mapping'ine ve behavior setine ekle; iki eski C01 node yeterli değildir.

5. **M01–M03 acceptance fixture'larını gerçek sözleşmeye getir.**

   **M01:** Writer'ın create-before-open/O_EXCL temeli korunur. Tek parameterized fixture içinde `source_exact`, `source_case_equivalent`, `source_descendant`, `existing_empty_output`, `existing_nonempty_output`, `windows_junction` vakalarını birbirinden bağımsız yeni root'larda çalıştır. Her çağrıdan hemen önce/sonra source ve mevcut output `relative path + bytes + SHA-256` envanteri exact eşit olur. Existing output'a gerçekten `write_exclusive_json` çağrısı yap; testin kendisinin dosyayı sonradan değiştirmesi kanıt değildir. Junction oluşturulamıyorsa skip değil INCOMPLETE/nonzero. Altı expanded node'u M01 REQUIRED behavior setine ekle.

   **M02:** Mevcut el yazımı lock, tek release, `test-python`, 1/1 count ve tekrar-karakter hash fixture'ını sil. Şu fixture tek kabul yoludur:

   - Raw campaign lock'u gerçek `run_capital_forward.campaign_lock` producer'ıyla sabit TEST env/saatte üret; producer bytes'ına `magic_number` veya `campaign_root` gibi test-only alan ekleme.
   - Pytest temp root'ta her vaka için üretilen ephemeral RSA TEST key altında iki `TEST_ONLY_SYNTHETIC_FIXTURE` release vardır: `R0` ve `R1`. Private key evidence/proposal'a girmez; trusted public key validator'a dışarıdan verilir. İkisinin ayrı tam ZIP, `.manifest.json` ve raw-manifest imzası bulunur. Manifestler gerçek builder schema/provenance'ını, farklı release_id/archive, 40-hex commit, `git_dirty=false`, Python 3.11, `pytest_passed_count=268`, `artifact_pytest_count=139` ve exact beş artifact testini taşır: `test_deployment_security.py`, `test_xm_mt5_forward.py`, `test_super1_xm_forward.py`, `test_check_mt5_flat.py`, `test_v16_deployment_contract.py`.
   - Synthetic genesis signed canonical payload içinde `previous_transition.status=GENESIS`, `previous_transition_hash=null`, `synthetic_genesis=true`, old.release=new.release=SHA256(raw R0 manifest), release binding R0 olur. Validator default `allow_synthetic_genesis=False` açılır; yalnız explicit test flag + pytest temp TEST root + dışarıdan verilen ephemeral trusted public key üçlüsü birlikteyse GENESIS kabul edilir. Production caller bu flag'i expose/forward edemez. Flag-only ve root-only ayrı negatif node olur; gerçek source GENESIS için UNRESOLVED/FAIL verir.
   - Successor old.release=SHA256(raw R0), new.release=SHA256(raw R1), release binding R1 ve genesis canonical raw bytes/hash/signature bağını taşır. Validator successor'dan genesis transition imzasını, R0 raw-manifest imzasını ve R0 ZIP↔manifest bağını recursive/doğrudan yeniden doğrular. `genesis.new == successor.old` release dışında runtime/harness/contract/calendar alanlarında da exact olur; bu hashlerin her biri ilgili gerçek fixture dosyasının bytes SHA-256'sına bağlanır.
   - Validator `hashes.old/new.release` değerlerini ilgili imzalanmış raw manifest SHA'sına bağlar; yalnız 64-char format kontrolü yetmez. R0/R1 ZIP↔manifest üyeleri 1:1; duplicate, non-normalized, traversal, credential/forbidden, eksik/fazla/değişmiş member ayrı exact hata verir.
   - Account/server/lock; old-new link; release binding; R0/R1 archive/member; genesis/R0 recursive trust; runtime/harness/contract/calendar bytes; snapshot member; frozen/risk; eksik alan; wrong trust root; replay; flag-only; root-only ve structural cycle negatiflerinin her birini ayrı fresh-signature node yap ve exact error code ara. Hepsini M02 REQUIRED behavior setine ekle.

   **M03:** Ortak terminal-R fonksiyonunu koru fakat saf fonksiyon tek başına magic/symbol/reward, OUT entry, SL/TP reason, open-position exclusion, duplicate position, `(time_msc,ticket)` sıra, tam sıralı terminal satırları, lookback son-N, state sum ve risk scale sonuçlarının tamamını exact `{ordered_full_terminal_rows,last_n,state_sum,risk_scale}` olarak döndürsün; runtime ile validator bu aynı sonucu doğrudan kullansın ve caller'lar ayrı filtre/sum/scale hesabı yapmasın.

   - Validator dışarıdan TEST-root ile imzası doğrulanmış campaign-root binding alsın: `expected_lock_sha256`, `expected_account_login`, `expected_server`, `expected_magic`, raw Super1 config relative path/bytes/SHA ve bu bytes'tan exact türetilen epic↔symbol↔reward map, `lookback`, `negative_scale`, `nonnegative_scale`, `terminal_history_days`, entry/OUT ve SL/TP sabitleri. Raw lock yalnız gerçek producer schema'sıyla doğrulansın; expected magic/risk parametreleri signed config/transition binding'den gelsin. Default reward veya caller-supplied serbest risk parametresi yasaktır. Source ve snapshot iki kez aynı saf fonksiyondan hesaplanıp bu dış otoriteye bağlansın. İki taraf birlikte yabancı fakat tutarlı account/server/magic/reward/lookback/scale ile değiştirilirse exact `campaign_root_binding.semantic` FAIL olsun.
   - Bar DB ve order DB için ayrı `wal_autocheckpoint=0`, açık writer ve checkpoint edilmemiş committed son kayıt fixture'ı kur. Bar satırı; order_intent; outbox event/ack/sequence ve broker_execution_state backup'ta bulunur. WAL'sız raw bar copy exact `minute_bars_and_conflicts.wal_continuity`; order copy exact `order_store.wal_continuity` verir.
   - Deal, position, magic/symbol, duplicate terminal, outbox sequence/ack/content, SUBMITTED/UNKNOWN/CANCEL_ARMED, zorunlu dosya/alan, signed raw-config path/SHA, reward map, lookback, iki scale, terminal-history-days, entry/OUT/SL/TP sabitleri ve external identity negatiflerinin her biri ayrı kopya, yeniden hesaplanmış manifest ve exact error code kullanır. Toplu loop + yalnız `status=FAIL` kabul değildir. Bütün node'ları M03 REQUIRED behavior setine ekle.

6. **O01 — tek production policy'yi gerçekten çalıştır; eski dead branch'i sil.**

   [Mevcut policy](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/super1_rollover_failure.ps1:102) callback dönüşlerini atıyor; [production](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/deploy/rollover_super1_campaign_windows.ps1:626) `TasksStopped=$false` hardcode ediyor; [harness](C:/Users/ISAAC/Documents/otobacktestprojesi/work/backtest/scripts/run_super1_rollback_harness.ps1:61) yeni policy yerine eski orchestration'ı çağırıyor. Şu tek algoritmayı uygula:

   - Policy girişte actual `BrokerSideEffectPossible`, actual `TasksStopped`, ayrı app/state/taskXML mutation bayrakları, fatal ve recovery latch preexistence durumlarını alır. Production en az `Restore`, `VerifyOldHashes`, `VerifyOldTaskXml`, `StartOldMain`, `StartOldWatchdog`, `VerifyOldHealth`, `StopOldMain`, `StopOldWatchdog`, `WriteLatch`, `WriteEvidence`, `WriteHealth` callback'lerini verir ve policy'yi exact bir kez çağırır. Her callback exact structured `PASS/FAIL` döndürür; null, throw veya PASS dışı değer FAIL'dir. Catch içindeki eski restore/restart/dead branch tamamen silinir.
   - Preexisting fatal veya recovery latch varsa hiçbirini temizleme/overwrite etme; safe rollback yasaktır. Sıra `StopOldWatchdog -> StopOldMain -> WriteLatch(existing/create-new result) -> WriteEvidence -> WriteHealth`; restore/start=0. WriteLatch yalnız eksik dosyayı create-new deneyebilir ve preexisting/created/failed ayrımını döndürür.
   - Preexisting latch yok ve `BrokerSideEffectPossible=true` ise sıra `WriteLatch -> StopOldWatchdog -> StopOldMain -> WriteEvidence -> WriteHealth`; restore/start=0. Callback null/hata/partial olsa da sonraki callback exact bir kez denenir.
   - `BrokerSideEffectPossible=false, TasksStopped=false, app/state/taskXML mutation=false` ise restore/restart çağırma; sıra `VerifyOldHashes -> VerifyOldTaskXml -> VerifyOldHealth` olur ve üçü PASS ise safe rollback'tir. Mutation, null/throw veya kanıt eksikliğinde sıra `StopOldWatchdog -> StopOldMain -> WriteLatch -> WriteEvidence -> WriteHealth` olur; iki stop best-effort exact birer kez, restart=0.
   - `BrokerSideEffectPossible=false, TasksStopped=true` ise sıra `Restore -> VerifyOldHashes -> VerifyOldTaskXml -> StartOldMain -> StartOldWatchdog -> VerifyOldHealth` olur. Her adım yalnız önceki PASS ise ilerler. Herhangi FAIL'de `StopOldWatchdog` ve `StopOldMain` best-effort exact birer kez denenir; sonra `WriteLatch -> WriteEvidence -> WriteHealth`, bir daha restart yok.
   - WriteLatch exact `{status,latch_preexisting,recovery_preexisting,latch_written,recovery_written,paths,errors}` döndürür; `status=PASS` yalnız iki kalıcı gate dosyası çağrı sonunda mevcutsa, aksi halde `FAIL` olur. `persistent_gate_proven` çağrı sonrası iki dosyadan en az biri gerçekten mevcutysa true'dur. Both-fail, fatal-only ve recovery-only üç ayrı unsafe sonuçtur.

   Harness aynı policy'yi şu 16 exact vakada çalıştırır: running/no-mutation safe; stopped safe restore; preexisting fatal; preexisting recovery; restore failure; old-hash failure; main-start failure; watchdog-start failure; old-health failure; post start-timeout; post watchdog failure; stop failure; gerçek evidence-writer I/O failure; both latch writes fail; fatal-only partial; recovery-only partial. Merkezi immutable case contract her vaka için giriş vektörünü (`brokerSideEffectPossible`, `tasksStopped`, üç mutation flag'i, iki preexisting latch), injected callback+structured sonucunu, exact ordered callback trace/counts, expected policy status, latch tuple ve process-gate exit'i ayrı yazar; policy bu expected tablosunu okuyamaz. Harness ve bağımsız Python oracle aynı contract'ı ayrı parserlarla doğrular; actual'dan expected türetmez. Böylece main-start/post-timeout ve watchdog-start/post-watchdog vakaları aynılaştırılamaz. Toplam vaka sayısını contract'tan dinamik ölç. Evidence I/O için gerçek erişilemez hedef kullan, yalnız `throw` yazma. Tek metrics nesnesi JSON/stdout/call-trace'i besler. Her vakada before/after app/state/taskXML/economic envanteri exact assert edilir.

   Startup gate broker/MT5 init'ten önce aynı gerçek state root'ta fatal **veya** recovery latch'ten biri varsa nonzero çıkar. Fatal-only ve recovery-only ayrı yeni Python process ile kanıtlanır; boş başka dizin negatif değildir. `both latch writes fail` sonucunu exact `UNSAFE_UNLATCHED`, `persistent_gate_proven=false` yaz; ortada latch olmadığı için bu vakada latch-consumed iddiası kurma ve asla rollback-safe sayma.

   PowerShell parser ve AST testleri production'ın policy'yi exact bir kez actual bayraklar ve bütün callback'lerle çağırdığını, eski catch restore/restart kodunun bulunmadığını ve harness'ın aynı fonksiyonu çağırdığını doğrular. Policy çağrısı kaldırılmış mutation kopyası FAIL olur. Bütün expanded vakalar O01 REQUIRED behavior setine eklenir.

7. **Runner, verifier, path, input ve baseline kapılarını fail-closed yap.**

   - `MANDATORY_INPUTS` içine gerçek `live_forward/xm_mt5_demo_config.json` ve architect-owned V08 required-node manifestini ekle; example config veya runner'ın ürettiği set bunun yerine geçmez. Capital, XM, Super1 configlerinin yalnız path/bytes/SHA'sını envantere al; secret içeriklerini evidence'e kopyalama. Missing/changed ignored file kesin FAIL'dir.
   - Run-012 baseline validation, harici harness ile aynı sealed test bytes/argv altında before-FAIL/after-PASS, `RUN010_UNBOUND/RUN012_UNBOUND`, baseline/current source-root SHA, test SHA, command, exit ve raw process artefaktlarını ayrı manifest üyeleri yap. Bunlar yoksa `evidence_payload_claim_ok=false`.
   - Tek `attempt_id` temp dizin oluşturulmadan önce üretilir; temp adı, bütün raw/sealed kayıtlar, prepublish/final sonuçları, receipt ve `.failed-<attempt_id>` adında aynı değer kullanılır. Hata dalında yeni UUID üretme.
   - `software_claim_ok` yalnız production davranış node'ları, targeted/full JUnit, compileall, calendar ve O01 parser/AST/harness sonuçlarından hesaplanır; reporter/verifier sonucu içine katılmaz. `continuation_fixture_claim_ok` yalnız M01–M03'tür ve software claim'e bağlanmaz. `evidence_payload_claim_ok` exact JUnit/required sets, raw observations, reporter negatives, input/predecessor/baseline, path portability, preseal, proposal/readiness içeriğinin verifier-öncesi claim'idir. `local_acceptance_claim_ok` bu üçünün AND'idir.
   - Preseal inventory kendisi ve final manifest hariç bütün mevcut candidate üyeleri exact path/bytes/SHA ile bağlar. Independent verifier actual tree'yi yeniden çıkarıp inventory ve `inventory_sha256` ile karşılaştırır; yalnız `preseal_integrity_ok=true` alanına güvenmez.
   - Independent verifier runner, observation writer veya reporter ölçüm fonksiyonunu import etmez. Prepublish modunda process stdout/stderr hash+exit, JUnit exact node/status/count, iki merkezi required set, raw identity/checkpoint/delta, expected/actual/recompute, mandatory input, predecessor, baseline, preseal, acceptance rows, proposal/readiness ve bütün claim formüllerini yeniden hesaplar. Final modunda test/producer tekrar çalıştırmadan aynı sealed bytes üzerinde bunların tamamını yeniden doğrular; ayrıca manifest 1:1 bytes, canonical basename/run_id ve canonical final path'i kontrol eder. `all_required_nodes_observed=true` boolean'ına güvenmek yasaktır.
   - Run-012'de 17 JSON/30 mutlak path ve stderr'de sekiz workspace/temp path vardır. Verifier manifestteki her üyeyi uzantıdan bağımsız inceler: NUL içermeyen geçerli UTF-8 veya UTF-8-BOM bytes text sayılır; diğer üyeler exact path + magic/MIME taşıyan kapalı binary allowlist'te değilse FAIL olur. Bütün text, separator ve case normalize edilerek recursive taranır; workspace absolute root, temp absolute root veya `.<actual_run_id>.tmp-*` dizesi tek yerde bile varsa FAIL. Dış receipt'in doğrulanmış canonical `final_path` alanı tek istisnadır. Calendar/continuation çıktıları repo-relative yazılır; harness bütün case path'lerini case-root relative yazar; beklenen Python hatası harness içinde yakalanıp structured relative error yapılır; reporter stdout relative output path verir.
   - Receipt candidate gate'lerini sealed claim'den kopyalamaz: `software_candidate_ok=software_claim_ok && final_semantic_verifier_ok`; `continuation_fixture_candidate_ok=continuation_fixture_claim_ok && final_semantic_verifier_ok`; `evidence_payload_candidate_ok=evidence_payload_claim_ok && prepublish_verifier_ok && final_semantic_verifier_ok`; local candidate üçünün AND'idir. `run_evidence_ok=local_acceptance_candidate_ok && delivery_integrity_ok`. Receipt candidate `attempt_id` ile prepublish/final verifier'ın canonical stdout JSON bytes'ını base64, bytes SHA-256, exit code, ilgili verifier source SHA-256 ve ayrı receipt-verifier source SHA-256 olarak gömer. Yalnız stdout SHA yazmak kabul değildir.
   - Final verifier receipt'i okumaz. Final PASS sonrasında ayrı read-only receipt verifier, henüz dış final adına taşınmamış receipt-candidate bytes'ını ve canonical run'u okur; gömülü stdout'ları decode eder, portability taramasına dahil eder, bütün gate formülleri/manifest/final-path/attempt/source-SHA bağlarını bağımsız yeniden hesaplar ve exact PASS/nonzero üretir. Runner/recorder/reporter ölçüm kodunu import etmez. Receipt kendi receipt-verifier stdout'unu taşımaz; böylece self-verification döngüsü kurulmaz.
   - Prepublish/final/receipt verifier ve publication state-machine bozma vakalarını V08 manifestindeki 33 explicit ID ile tek `test_v03_evidence_and_publication_protocol_case[...]` fixture'ında çalıştır. Bunlar path türleri, preseal, JUnit, stale/normatif set, raw identity/checkpoint, mandatory config, baseline, predecessor, claim, manifest, decoded iki stdout, iki verifier source SHA, receipt I/O, attempt-id ayrışması, final/receipt quarantine, canonical collision ve verified-receipt öncesi authority vakalarının tamamıdır. Bilerek bozuk fixture bytes sealed candidate'a kopyalanmaz; sealed rapor yalnız relative case id, mutation source SHA ve exact error code taşır. Her expanded node sorumlu subprocess için nonzero ve tek exact error code assert eder; observation üretmeden V03/N setine girer.

8. **Release proposal ve readiness'i dürüst üret.**

   [Mevcut proposal](C:/Users/ISAAC/Documents/otobacktestprojesi/outputs/super1_readiness_20260831/run-012/continuation-contract-proposal.json:1) 34 üyeli required closure ile prospective archive listesini birebir aynı yazmaktadır; builder bütün dizinleri stage ettiği için bu doğru değildir.

   - `required_archive_member_closure`, runtime ZIP'in base+Super1 zorunluları ve local import/dot-source/config bağımlılıklarının exact union'udur. Gerçek `xm_mt5_demo_config.json`, `backtest/__init__.py`, watchdog/run-forward/check-flat, pyproject/README/lock, bütün Super1 deploy scriptleri ve runtime helper'lar eksik kalamaz.
   - `build_and_artifact_test_input_inventory`, ZIP'e girmeyen builder/test/provenance girdilerini; bu turdaki test ve doküman değişikliklerini; artifact testlerini exact path/bytes/SHA ile ayrı listeler. Builder test sonrası `artifact_tests/**` girdilerini sildiği için bunları prospective archive üyesi sayma; ancak successor builder contract açıkça değiştirilirse yeni contract altında dahil edilebilir.
   - `prospective_post_cleanup_archive_inventory`, builder'ın copy+generated kurallarından `artifact_tests`, cache, compiled ve secret exclusion'ları çıkarıldıktan sonra paket üretmeden hesaplanan mevcut static üyeleri listeler. Her üye exact tek source rule, relative source/path, bytes ve SHA taşır; required closure veya build-input envanteriyle yapay olarak eşitlenmez.
   - Set kapısı exact `required_resolved ⊆ prospective_static`, `required_unresolved == unresolved_generated_members`, `prospective_static ∩ unresolved_generated_members = ∅` arar. Missing, duplicate, ruleless veya birden çok source-rule'a bağlı üye FAIL'dir. Builder'ın üreteceği/download edeceği wheel/lock bytes'ı yerelde yoksa path/hash uydurma; `unresolved_generated_members` içinde `DEPENDENCY_BYTES_NOT_AVAILABLE_WITHOUT_BUILD` yaz. Status `INCOMPLETE_NOT_BUILT`, eski v16 gate `NOT_RUN_CONTRACT_MISMATCH`, proposal ve successor contract `SUCCESSOR_CONTRACT_CHANGE_REQUIRED` olur.
   - Eski v16 sözleşmesi exact beş artifact dosyasını, 268/139 sayılarını ve `unchanged=NOT_VERIFIED` durumunu tarihsel veri olarak taşır. Successor'ın genişletilmiş test listesi eski release integrity ile uyumsuzken `LOCAL_ONLY_NOT_BUILT` yazma; exact `SUCCESSOR_CONTRACT_CHANGE_REQUIRED` blocker'ı ver. M02 `TEST_ONLY_SYNTHETIC_FIXTURE` ZIP/imzası bu statüleri değiştiremez; production successor ZIP/build/sign yoktur.

   Readiness exact şu alanları taşır: `overall=NO_GO`, `decision=NO_GO`, `campaign_mode=CURRENT_SUPER1_CAMPAIGN_CONTINUATION`, `safe_to_apply=false`, `apply_allowed=false`, `proven=false`, `parity=false`; source/target runtime `NOT_VERIFIED`; source fence/snapshot/target environment `NOT_VERIFIED`; broker/pending/fill-exit/task/deployment/release `NOT_RUN`; successor release `NOT_BUILT`, signature `NOT_SIGNED`. `fresh-forward profitability and historical parity are unproven` teknik NO_GO nedeni değildir ve silinir. Yerel açıklar kapanana kadar exact local blocker'lar; kapandıktan sonra yalnız gerçek ortam `NOT_RUN/NOT_VERIFIED` nedenleri yazılır.

   Acceptance matrix yalnız claim/count özeti olamaz. Her requirement code için exact required node listesi, behavior/evidence-negative sınıfı, iki JUnit durumu, raw/mutation artefaktı, expected, independently recomputed actual, gate sonucu ve hata kodu bulunur.

9. **Yeni koşunun yayın kapısı.**

   Run-012'nin 47 unique/53 mapping seti V08 manifestinde strict alt küme olarak aynen korunmuştur; eski toplu testlerin nodeid'lerini silme veya yeniden adlandırma, gövdelerini bu belgedeki gerçek fixture/oracle ile güçlendir. Normatif exact sonuç `B=164`, `N=53`, unique `217`, mapping edge `225`, suite başına required PASS `217`, suite başına behavior observation `164`, iki suite toplam observation `328` olur. Targeted/full toplam testcase sayısını koşudan ölç; `181/359` değerlerini yeniden kullanma. `B ∩ N=∅`; targeted/full için `nodeids(JUnit) ∩ (B∪N)=B∪N` ve her required üye exact bir kez; observation seti exact `B`, `N` observation sayısı sıfırdır. Manifest SHA veya tek invariant bozulursa claim false olur; `>=` kabul edilmez.

   Yayın sırası exact şudur: bütün local claim'ler ve prepublish verifier PASS; manifest final üye olarak create-new/fsync; temp candidate aynı volume içinde atomic olarak canonical run adına rename edilir fakat henüz authoritative değildir; final verifier canonical dizini salt okunur doğrular ve hiçbir producer/test yeniden çalıştırmaz. Final FAIL ederse canonical dizin aynı `attempt_id` taşıyan `.failed-<attempt_id>` adına atomic çekilir, receipt oluşturulmaz ve süreç nonzero çıkar. Final PASS sonrasında dış receipt bytes'ı sibling temp path'te create-new/fsync edilir; read-only receipt verifier bu exact bytes ve canonical run üzerinde PASS ederse temp receipt final `.verification.json` adına no-overwrite atomic rename edilir ve ancak o anda koşu authoritative olur. Receipt verification, final-name collision veya partial-write FAIL'inde receipt candidate/kalıntısı `.failed-receipt-<attempt_id>` olarak korunur, canonical aynı attempt'ın failed adına çekilir ve süreç nonzero çıkar. İlk boş kimlik yalnız canonical/temp/receipt/failed adlarının tamamı yoksa boştur; run-013 boşsa onu, değilse ilk sonraki gerçekten boş kimliği seç.

   Teslim en az şunları içerir: exact acceptance matrix; readiness; evidence report; process metadata; targeted/full JUnit; gerçek behavior observation/raw kaynakları; evidence/verifier mutation raporları; run-012 baseline validation ve before-FAIL/after-PASS; M01–M03 fixture kanıtları; 16 O01 case contract/raporu, AST/parser ve inventory; üç ayrı release envanteri + unresolved members; mandatory input before/after; predecessor before/after; full-text portability scan; preseal; final manifest; dış receipt. Run-012/run-011/run-010 ve failed sibling before/after hashlerini kaydet.

   Bu yerel tur başarıyla yayımlansa bile genel karar `NO_GO` kalır. Kendiliğinden SSH, kaynak/hedef/broker preflight, transfer, kurulum, task veya deployment başlatma; mimarın sonraki talimatını bekle.
