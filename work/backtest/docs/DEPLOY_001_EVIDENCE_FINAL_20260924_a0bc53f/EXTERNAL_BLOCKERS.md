# Dış kabul engelleri ve owner aksiyonları

Bu maddeler yerel test sonucu ile kapatılamaz. Gerçek broker’a order gönderme veya canlı bot başlatma talimatı değildir.

| Kapı | Sorumlu / gereken artifact | Eksik kanıt ve kabul koşulu | Sonraki komut / işlem |
|---|---|---|---|
| Güncel bağımsız inceleme | Bağımsız reviewer; final code SHA/tree ve bu paketin hash’leri | a350263 kararı `CHANGES_REQUIRED`; a0bc53f kod ref’i henüz bağımsız incelenmedi. Review kararı ve bulgu bazında disposition gelmeden push/merge yok. | Review paketi: `FINAL_ACCEPTANCE.md`, `RISK_MATRIX.md`, `manifest.json`; reviewer raporunda exact reviewed commit/tree zorunlu. |
| GitHub CI | Repo/runner owner; `[self-hosted, Windows, risk-hardened]` runner, 144 ham girdi kökü, ayrı engine-audit kökü/manifest, owner symlink fixture | Workflow mevcut; runner erişimi/remote Actions sonucu yok. Varsayılan hosted runner bu yerel dosyaları taşımaz. | Owner authenticated host’ta, path’leri **runner’ın kendi yerel mutlak yolları** ile: `gh workflow run super1-risk-gates.yml --ref <approved-pushed-ref> --field risk_provenance_source_root=<runner-144-input-root> --field engine_audit_source_root=<runner-engine-audit-root> --field symlink_fixture_root=<verified-owner-fixture-root>`; başarılı run URL ve artifact SHA’larını ekle. `gh` bu oturumda yok. |
| Private identity `identity-full` | Repo/security owner; korumalı `BROKER_IDENTITY_DENYLIST_JSON` veya eşdeğer özel JSON ve kabul edilmiş history baseline | Denylist içeriği okunmadı/çıktıya yazılmadı; bu oturumda environment variable ve `gh` CLI yok. Sadece structural scan sonucu PASS/0. | Owner’ın secure path’iyle: `python scripts/scan_public_broker_identity.py --root . --candidate-ref a0bc53f3f7e9735adc90eaea1899b7cdd1bde4d2 --denylist <secure-file-outside-repo> --baseline security/broker_identity_history_baseline.json --report <evidence-outside-repo>/identity-full.json`. Kabul: exact candidate-tree denylist `0`, structural `0`; raw değerleri paylaşma. |
| Main entegrasyonu | Bağımsız kabul + repo maintainer/branch protection | Uzak `origin/main` gözlemi `c1c769aec68b84c9c13f9add198ab130241c69ad`; branch bu commit’in descendant’i ve kod ref’inde 72 commit ahead. Hiç push/merge yapılmadı. | Önce exact a0bc53f review kabulü; sonra normal PR/push ve zorunlu CI/branch protection. Force push/history rewrite yok. Main SHA değişirse conflict çözümü ve ilgili testler yeniden incelenir. |
| Signed Windows release, upgrade, rollback | Yetkili release owner; accepted main/source/tree, public trust pin, yetkili DPAPI erişimi, admin target, hedef/service/task/ACL öncesonrası snapshot, rollback gözlemi | Bu oturum admin değil. Yerel private-key path’i varlığı görüldü fakat anahtar açılmadı/kullanılmadı. Accepted main, public-key trust pin doğrulaması, target/owner snapshot ve rollback yürütmesi yok. | Yalnız accepted main ve elevated doğru owner oturumunda: `pwsh -NoProfile -File .\deploy\build_signed_windows_release.ps1 -Profile super1 -OutputArchive <signed-archive-path> -Python <clean-python-3.11> -SymlinkFixtureRoot <verified-owner-fixture-root> -PrivateKeyPath <authorized-DPAPI-key-path>`. Ardından release integrity, upgrade ve rollback runbook’larını hedefte uygula; gerçek order/bot başlatma yok. |
| Promotion 12–14 | Dukascopy/provider owner; yazılı rate-limit politikasının sürümü, request spacing/concurrency, UTC gözlem penceresi, izinli normal HTTPS response/terminal ledger | Gerçek provider çağrısı veya owner/provider belgesi yok. Kontrollü yerel mock testleri dış kabul değildir; gerçek provider’da 429/5xx üretme şartı konmaz. | Normal erişime izin veren owner onayı ve observation window sağlanınca mevcut promotion kod/test branch’inde provider ledger’ını çalıştır; HTTP response hash/timestamp/terminal state ve request budget’ı kaydet. Bu branch’te promotion audit CLI yok; flag uydurulmadı. |
| Promotion 15/19 | Proje/provider owner; 113 satırlı değiştirilmemiş frozen inventory, canonical V5 `COMMITTED` manifest, her hedefin bundle/manifest/raw/derived SHA ve gerçek residual değerlendirmesi | Güncel canonical V5 manifest, provider ledger ve residual-zero sonucu yok. Yeni residual sayısı **hesaplanmadı**; tarihsel snapshot sayıları bugünkü kabul olarak kullanılmıyor. | Owner, inventory SHA/113 target set’ini sabitleyip gerçek provider sonuçlarını bağlasın; `scripts/audit_dukascopy_reacquisition_v5.py` bu checkout’ta bulunmadığından uygulanacak branch ve CLI owner’dan alınmalı. Kabul: 113/113, `COMMITTED`, residual `0`, target-CAS/hash bağları. |
| Promotion 16/17 | Owner trust/security; public key + trust pin, detached RSA-PSS imza, source/tree/run nonce/event-root, replay ledger, current auditor/node-helper SHA, immutable evidence root | Güncel imzalı owner attestation/evidence root yok; eski hash eşleşmeleri bu ref’e taşınmadı. | Owner imzalı paketi teslim etsin; doğru promotion auditor’ında detached signature, source/tree, nonce/event-root/replay ve tüm path/content hash’lerini doğrula; eski attestation’ı yeniden yazma, gerekirse re-attestation al. |
| Promotion 18 | Broker/account owner; gerçek DEMO/account-switch read-only snapshot, terminal/account binding ve UTC gözlem | Gerçek broker oturumu yok; hiçbir MT5 order/write çağrısı yapılmadı. | Owner-controlled demo terminalde read-only account-switch observation al; write API’lerini proxy/trap ile reddet; gerçek order göndermeden hash’li raporu bağla. |

`DEPLOYMENT_READY=false` kalır. Bu listedeki hiçbir boşluk sentetik fixture, local key, geçmiş snapshot veya branch test başarısıyla doldurulamaz.
