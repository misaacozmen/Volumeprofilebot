# Eksik dış girdiler — tek owner listesi

| Kapı | Owner ve gereken gerçek girdi | Durum / yeniden çalıştırma |
|---|---|---|
| Bağımsız kabul | Bağımsız reviewer: exact final commit/tree, manifest ve ham koşular | Evidence commit hazırlandıktan sonra ayrı inceleme; kabul gelmeden push yok. Son karar dış reviewer raporunda. |
| GitHub CI | Repo/runner owner: online `[self-hosted, Windows, risk-hardened]`, CPython 3.11, 144 hash-pinned raw kökü, ayrı 2025 audit kökü, mevcut owner symlink fixture | Authenticated API: 0 registered runner, 0 repository variable. `BROKER_IDENTITY_DENYLIST_JSON` secret adı mevcut. Owner runner'ı ve üç workflow input/variable'ı sağlamalı; accepted pushed ref'te `super1-risk-gates.yml` çalışmalı. Runner kayıt/servis kurulumu yapılmadı. |
| Main | Maintainer + tüm zorunlu CI kapıları | main c1c769aec68b84c9c13f9add198ab130241c69ad. Bağımsız kabul sonrası normal push/PR; CI PASS olmadan merge yok. Source-changing conflict yeniden test/review gerektirir. Main sonucu ayrıca full suite ve private scan ile doğrulanmalı. |
| Private identity | Security owner: mevcut protected denylist + değişmemiş accepted baseline | Yerel code ref PASS, 0 yeni history / 0 current denylist / 0 structural. Exact final ref ayrıca taranır. Ayrıntılı private raporlar repo dışında; baseline'a bulgu eklenmedi. |
| Provider kökeni | Data/provider owner: 144 pre-2025 ve iki 2025 dosyası için özgün download receipt ve retrieval UTC timestamp | Yerel hash/window/deterministic audit PASS, provider kabulü değil. Makbuz eksik. Aynı `prepare_risk_provenance.py --source-root <144-root> --engine-audit-source-root <2025-root>` komutuyla tekrar doğrula. |
| Signed Windows release/upgrade/rollback | Release owner: accepted main/tree, gerçek public trust pin, yetkili DPAPI key erişimi, elevated hedef oturum, service/task/ACL/owner snapshot ve rollback hedefi | Bu oturum admin değil; key kullanılmadı. `pwsh -NoProfile -File deploy/build_signed_windows_release.ps1 -Profile super1 -OutputArchive <archive> -Python <clean-python311> -SymlinkFixtureRoot <owner-fixture> -PrivateKeyPath <authorized-DPAPI-key>`; signing gate korunur. Sonra mevcut upgrade/rollback runbook; yanlış hash/imzada işlem öncesi ret, stop/lock/cleanup ve güvenli rollback kanıtı şart. |
| Promotion 12–14 | Provider owner: gerçek rate-limit policy sürümü, request budget/concurrency/spacing, UTC observation window, normal HTTPS response/terminal ledger | Gerçek provider ledger yok; sentetik 429/5xx dış kabul sayılmaz. Yetkili promotion branch/auditor ve owner gözlem girdileri sağlanmalı. |
| Promotion 15/19 | Proje/provider owner: frozen 113-target inventory SHA, canonical V5 COMMITTED manifest, raw/derived/bundle hash ve gerçek residual hesabı | Bu checkout'ta `audit_dukascopy_reacquisition_v5.py` yok; gerçek manifest/ledger verilmedi. Güncel residual hesaplanamadı. 113/113 hedef ve residual 0 doğrulanmalı; eski sayılar taşınmadı. |
| Promotion 16/17 | Security/attestation owner: public key/trust pin, detached RSA-PSS, source/tree/run nonce/event-root, replay ledger, güncel auditor/helper SHA | Güncel imzalı owner paketi yok. Gerçek re-attestation gerekir; eski imza/hash yeniden yazılmaz. |
| Promotion 18 | Broker/account owner: gerçek DEMO/account-switch read-only snapshot, terminal/account binding, UTC gözlem | Broker oturumu yok. Owner-controlled read-only gözlem ve write API trap gerekir; order gönderilmez. |

`DEPLOYMENT_READY=false`. Kod, OS, bağımsız inceleme, main, CI, deployment ve promotion ayrı kapılardır. Canlı bot/order başlatılmadı.
